
Project Root: C:\Users\0219_PyWeb\Desktop\albatross
--------------------------------------------------
File: \accounts\migrations\__init__.py
--------------------------------------------------
File: \accounts\static\accounts\css\accounts_styles.css
/* chart_styles.css (とか、マサ君が使ってるCSSファイル) のどこか分かりやすい場所に追記 */

/* --------------------------------------------- */
/* Accounts App - Account Delete Form Styles     */
/* (アカウント削除フォームのスタイル)              */
/* --------------------------------------------- */

/* ユーザー名ラベルを折り返さないようにする */

.username-label-nowrap {
    white-space: nowrap; /* テキストを折り返さずに1行で表示する */
} 

 .form-card h2 { /* ← .login-form-card h2, .signup-form-card h2 の両方に適用されるように */
      margin-top: 0; /* カードのパディングで調整するなら、h2自体のマージンは0にしたり */
      margin-bottom: 1.5rem; /* 下のマージンはBootstrapのデフォルト (mb-4) と合わせるか調整 */
    }


    /* =============================================== */
/* ヘルプテキスト (注意書き) のためのスタイル       */
/* (主に signup.html で使う想定)                 */
/* =============================================== */

.help-text-custom {
    /* 文字の大きさを少し小さくする */
    font-size: 0.875em; /* Bootstrapの .form-text と同じくらいのサイズ感 */

    /* 文字の色を少し薄いグレーにする */
    color: #6c757d !important; /* Bootstrapの .text-muted と同じ色 */
                            /* !important は、他のスタイルに負けないように念のため付けてるけど、
                                  なくても効くなら外してもOK！ */

    /* ブロック要素として表示して、入力欄の下に来るようにする */
    /* (HTML側で <small> タグとか <p> タグで囲んでれば、
       デフォルトでブロックっぽく表示されることも多いけど、念のため) */
    display: block;

    /* 上の要素 (入力欄とか) との間に少し余白を空ける */
    margin-top: 0.25rem;

    /* ★★★ ここが今回のキモ！ ★★★ */
    /* 親要素の幅を超えないように、最大幅を100%に設定 */
    width: 100%;
    max-width: 100%;

    /* もし、特定の幅で必ず折り返したいなら、こんな感じでピクセル指定もできるよ */
    /* max-width: 300px; */ /* ← この値は、実際の入力欄の幅とかに合わせて調整してね！ */
                           /*    でも、レスポンシブデザインを考えると、まずは 100% で試すのがオススメ！ */

    /* 長い単語やURLなどがはみ出すのを防ぐために、強制的に折り返す設定 */
    /* (基本的には、日本語ならあまり気にしなくても大丈夫だけど、念のため) */
    word-wrap: break-word;     /* 古いブラウザ向け */
    overflow-wrap: break-word; /* 新しい標準的な書き方 (両方書いとくと安心) */
    background-color: yellow !important; 
    border: 2px solid red !important;   
    width: 100%; 
    box-sizing: border-box !important; 
}

/* もし、ヘルプテキストが ul > li 構造になってる場合 (パスワードの注意書きとか) のスタイル */
.help-text-custom ul {
    padding-left: 1.2rem;  /* 箇条書きの左側のインデントを調整 */
    margin-bottom: 0;      /* 下の余白をなくす (もしあれば) */
    list-style-type: disc; /* 点のマーカーにする (デフォルトかもだけど念のため) */
}

.help-text-custom ul li {
    margin-bottom: 0.1rem; /* 各箇条書き項目の下の余白をほんの少し */
}
--------------------------------------------------
File: \accounts\templates\accounts\account_delete_complete.html
{% extends "albatross_app/base.html" %} {# baseを継承するか、もっとシンプルなHTMLにするかはお好みで #}

{% block title %}アカウント削除完了{% endblock %}

{% block content %}
<div class="container mt-5 text-center">
    <div class="alert alert-success" role="alert">
        アカウントは正常に削除されました。
    </div>
    <p>5秒後にこのタブは自動的に閉じられます。</p>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            console.log("アカウント削除完了ページ表示中。5秒後にタブを閉じます。");
            setTimeout(function () {
                window.close();
            }, 5000);
        });
    </script>
</div>
{% endblock %}
--------------------------------------------------
File: \accounts\templates\accounts\account_delete_form.html
{% extends "albatross_app/base.html" %}

{% block title %}アカウント削除{% endblock %}

{% block content %}
<div class="container mt-5">
    <p class="text-end">こんにちは、{{ user.username }} さん！</p>
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h2 class="mb-0">アカウント削除</h2>
                </div>
                <div class="card-body">
                    {% if user_deleted_and_needs_logout %}
                    <!-- パスワードおっけーの場合 5/21---↑ユーザーが消えてない条件追加-->

                    <!-- アカウント削除が完了した場合の表示 -->
                    <div class="alert alert-success" role="alert">
                        アカウントは正常に削除されました。
                    </div>
                    <p>お疲れさまでした。このタブは自動的に閉じられます。</p>
                    <!-- <p><a href="{% url 'accounts:login' %}" class="btn btn-primary">ログインページへ</a></p> -->
                    <!-- 上の行は自動で閉じる設定にするためコメントアウト -->

                    
                    {% elif show_delete_modal_on_load %}
                    <p>アカウントを削除するには、セキュリティのため、現在のパスワードを入力してください。</p>
                    <div class="mb-3">
                        <label class="form-label me-2">ユーザー名：</label>
                        <span class="form-control-plaintext"><strong>{{ request.user.username }}</strong></span>
                    </div>
                    <div class="mb-3">
                        <label for="id_password_dummy" class="form-label">パスワード:</label> {# idは元のと被らないように変更 #}
                        <!-- {# ↓↓↓ パスワード入力欄は表示するけど、入力不可 (readonly) にしておくとか、値を空にするとか #} -->
                        <input type="password" class="form-control" id="id_password_dummy" readonly value="********">
                        <!-- {#valueは適当なダミー #} -->
                        <!-- {# または、単に {{ form.password }} を表示するだけ (ビューから空のフォームが渡ってれば) #} -->
                    </div>
                    <!-- {# <button type="submit" class="btn btn-warning">パスワード認証</button> ← このボタンは不要 #} -->
                    <button type="button" class="btn btn-secondary" onclick="window.location.href = '{% url 'accounts:account_delete' %}';">キャンセル（タブを閉じる）</button>
                    <!-- {# </form> #}
                    {# ▲▲▲ ここまでが「以前のパスワード入力画面」の再現 ▲▲▲ #} -->

                    {% else %}

                    <!-- 通常時入力フォーム表示 -->
                    <p>アカウントを削除するには、セキュリティのため、現在のパスワードを入力してください。</p>
                    {% if form.errors %}
                    <div class="alert alert-danger">
                        <p>入力内容に誤りがあります：</p>
                        {{ form.non_field_errors }}
                        {% for field in form %}
                        {% if field.name == "password" %}
                        {% for error in field.errors %}
                        <p>{{ field.label_tag }} {{ error }}</p>
                        {% endfor %}
                        {% endif %}
                        {% endfor %}
                    </div>
                    {% endif %}
                    <!-- ↓はパスワード認証用のフォーム -->
                    <form method="post">
                        {% csrf_token %}
                        <!-- 5/19---ここからユーザー名表示部分変更 -->
                        <div class="mb-3 d-flex align-items-baseline">
                            <label class="form-label me-2 username-label-nowrap">ユーザー名：</label>
                            <span class="form-control-plaintext"><strong>{{ request.user.username }}</strong></span>
                            <!-- ログイン中のユーザーを表示するだけに変更 -->
                        </div>
                        <!-- 5/19---パスワード入力部分ラベル表示調整 -->
                        <div class="mb-3">
                            <label for="{{ form.password.id_for_label }}" class="form-label">パスワード:</label>
                            {{ form.password }}
                        </div>
                        <button type="submit" class="btn btn-warning">パスワード認証</button>
                        <button type="button" class="btn btn-secondary" onclick="window.close();">キャンセル（タブを閉じる）</button>
                        <!-- キャンセルボタンのリンク先は、適切な場所（例：設定画面とか）に後で変更 -->
                    </form>
                    {% endif %}
                    <form method="post" id="actualDeleteForm" style="display: none;">
                        {% csrf_token %}
                        <input type="hidden" name="execute_delete" value="true">
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="deleteAccountConfirmModal" tabindex="-1" aria-labelledby="deleteAccountConfirmModalLabel"
    aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteAccountConfirmModalLabel">アカウント削除の最終確認</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                本当にアカウントを削除してもよろしいですか？<br>
                この操作は取り消せません。
            </div>
            <div class="modal-footer">
                <!-- {# ▼▼▼ モーダルのキャンセルボタンにidを付与！ ▼▼▼ #} -->
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"
                    id="cancelDeleteFromModalButton">キャンセル</button>
                <button type="button" class="btn btn-danger" id="confirmDeleteExecuteButton">はい、削除します</button>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        // --- アカウント削除完了＆ログアウト処理 ---
        {% if user_deleted_and_needs_logout %}
        console.log("アカウント削除完了メッセージ表示中。5秒後にログアウトしてタブを閉じます。");
        setTimeout(function () {
            function getCookie(name) {
                let cookieValue = null;
                if (document.cookie && document.cookie !== '') {
                    const cookies = document.cookie.split(';');
                    for (let i = 0; i < cookies.length; i++) {
                        const cookie = cookies[i].trim();
                        if (cookie.substring(0, name.length + 1) === (name + '=')) {
                            cookieValue = decodeURIComponent(cookie.substring(name.length + 1));
                            break;
                        }
                    }
                }
                return cookieValue;
            }
            const csrftoken = getCookie('csrftoken');

            fetch("{% url 'accounts:logout' %}", {
                method: 'POST',
                headers: {
                    'X-CSRFToken': csrftoken,
                    'Content-Type': 'application/x-www-form-urlencoded',
                }
            })
                .then(response => {
                    if (response.ok) {
                        console.log("ログアウトリクエスト成功。タブを閉じます。");
                    } else {
                        console.warn("ログアウトリクエストは成功しませんでしたが、タブを閉じます。Status:", response.status);
                    }
                    window.close();
                })
                .catch(error => {
                    console.error('ログアウトリクエスト中にエラー:', error);
                    window.close();
                });
        }, 5000);
        {% endif %}

        // --- パスワード認証成功時のモーダル自動表示 ---
        {% if show_delete_modal_on_load %}
        const deleteModalElement = document.getElementById('deleteAccountConfirmModal');
        if (deleteModalElement) {
            const deleteModal = new bootstrap.Modal(deleteModalElement);
            deleteModal.show();
            console.log("パスワード認証成功。アカウント削除確認モーダルを自動表示しました。");

            // モーダル内の「キャンセル」ボタンが押された時の処理
            const cancelBtnModal = document.getElementById('cancelDeleteFromModalButton');
            if (cancelBtnModal) {
                cancelBtnModal.addEventListener('click', function () {
                    console.log("モーダルのキャンセルボタンが押されました。アカウント削除ページにリダイレクトします。");
                    // モーダルを明示的に隠す (data-bs-dismiss="modal" があれば通常は不要だけど念のため)
                    // const myModalInstance = bootstrap.Modal.getInstance(deleteModalElement);
                    // if (myModalInstance) {
                    //     myModalInstance.hide();
                    // }
                    // ページをリロードしてパスワード入力画面に戻る (ビューのGETが呼ばれる)
                    window.location.href = "{% url 'accounts:account_delete' %}";
                });
            }
            // (オプション) モーダルの右上の×ボタンやEscキーで閉じられた時の処理も入れたいなら、
            // deleteModalElement.addEventListener('hidden.bs.modal', function (event) { ... }); を使う。
            // ただし、その場合は「はい、削除します」で閉じられた場合と区別するロジックが必要になる。
        }
        {% endif %}

        // --- モーダル内の「はい、削除します」ボタンの処理 (これは常時設定) ---
        const confirmDeleteExecuteBtn = document.getElementById('confirmDeleteExecuteButton');
        if (confirmDeleteExecuteBtn) {
            confirmDeleteExecuteBtn.addEventListener('click', function () {
                const actualDeleteForm = document.getElementById('actualDeleteForm');
                if (actualDeleteForm) {
                    console.log("モーダル経由でアカウント削除を実行します。");
                    actualDeleteForm.submit();
                }
            });
        }
    });
</script>
{% endblock %}
--------------------------------------------------
File: \accounts\templates\accounts\login.html
{% extends "albatross_app/base.html" %}
{% load static %}

{% block title %}ログイン{% endblock %}

{% block content %}
<div class="login-page-container d-flex flex-column justify-content-center align-items-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="login-form-card p-4 p-sm-5">
                    <h2 class="mb-4 text-center">ログイン</h2>

                    {% if form.errors %}
                    <div class="alert alert-danger" role="alert">
                        ユーザー名またはパスワードが正しくありません。
                    </div>
                    {% endif %}

                    <form method="post">
                        {% csrf_token %}
                        <!-- {{ form.as_p }} -->
                        <div class="mb-3">
                            <label for="{{ form.username.id_for_label }}" class="form-label">{{ form.username.label}}</label>
                            <input type="text" name="{{ form.username.name }}" id="{{ form.username.id_for_label }}" class="form-control" required>
                            {% for error in form.username.errors %}<div class="invalid-feedback d-block">{{ error }}
                            </div>{% endfor %}
                        </div>
                        <div class="mb-3">
                            <label for="{{ form.password.id_for_label }}" class="form-label">{{ form.password.label}}</label>
                            <input type="password" name="{{ form.password.name }}" id="{{ form.password.id_for_label }}" class="form-control" required>
                            {% for error in form.password.errors %}<div class="invalid-feedback d-block">{{ error }}
                            </div>{% endfor %}
                        </div>
                        <!--  ↑↑↑ ここまで個別レンダリングの例。{{ form.as_p }} のままでもOK！ ↑↑↑  -->

                        <!--  ↓↓↓ ボタンのd-gridのスペルミス修正！ (d-gird -> d-grid) ↓↓↓  -->
                        <div class="d-grid gap-2 mt-4"> {# mt-3 -> mt-4 で少し上に余白増やしてもいいかも #}
                            <button type="submit" class="btn btn-primary btn-lg">ログイン</button> {# btn-lgで少し大きくする例 #}
                        </div>
                    </form>
                    <!-- 下記の波カッコの処理でユーザーがログイン状態かどうか判断、新規登録を表示するかどうかの処理 -->
                    {% if not user.is_authenticated %}
                    <p class="mt-3 text-center">
                        アカウントをお持ちでないですか？ <a href="{% url 'accounts:signup' %}">新規登録はこちら</a>
                    </p>
                    {% endif %}
                </div>
            </div>
        </div>
        {% endblock %}
--------------------------------------------------
File: \accounts\templates\accounts\password_change_done.html
{% extends "albatross_app/base.html" %}

{% block title %}パスワード変更完了 - アルバトロス{% endblock %}

{% block content %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body text-center">
            <h2 class="card-title mb-4">パスワード変更完了しました！</h2>
            <p class="card-text">
                パスワードの変更が正常に完了しました。
                </p>
                <p class="card-text mt-3">
                    <strong>このタブ（ウィンドウ）を閉じて、元の画面に戻り、新しいパスワードで再度ログインしてください。</strong>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

<!-- メッセージを明確に！: 「このタブ（ウィンドウ）を閉じて、元の画面に戻り、新しいパスワードで再度ログインしてください。」って、
太字とか使って、ユーザーが次に何をすればいいか、はっきり分かるようにする。
余計なリンクは置かない！: このページからは、どこか別のページに遷移させるんじゃなくて、
「このタブを閉じる」っていう操作を促すのが目的なので、あえて他のリンクは置かない方が、ユーザーは迷わないかも。 -->
--------------------------------------------------
File: \accounts\templates\accounts\password_change_form.html
{% extends "albatross_app/base.html" %}

{% block title %}パスワード変更 - アルバトロス{% endblock %}

{% block content %}
<div class="container mt-5">
    <p class="text-end">こんにちは、{{ user.username }} さん！</p>
    <div class="row justify-content-center">
        <div class="col-md-8">
            
            <h2 class="mb-4 text-center">パスワード変更</h2>

            <form method="post">
                {% csrf_token %}

                {{ form.as_p }}

                <div class="d-grid gap-2 mt-3">
                    <button type="submit" class="btn btn-primary">変更する</button>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
--------------------------------------------------
File: \accounts\templates\accounts\signup.html
{% extends "albatross_app/base.html" %}
{% load static %}

{% block title %}新規ユーザー登録{% endblock %}

{% block content %}
<div class="login-page-container d-flex flex-column justify-content-center align-items-center">
    <div class="container"> <!-- container はあってもなくてもOKだけど、ログインと合わせるなら入れる -->
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="signup-form-card p-4 p-sm-5"></div>
                    <h2 class="mb-4 text-center">新規ユーザー登録</h2>

                    <form method="post">
                        {% csrf_token %}

                    <!-- {{ form.as_p }}でユーザークリエイトフォーム表示 -->
                    <!-- 下記は各登録の項目 -->
                        <div class="mb-3">
                            <label for="{{ form.username.id_for_label }}" class="form-label">{{ form.username.label }}</label>
                            <input type="text" name="{{ form.username.name }}" id="{{ form.username.id_for_label }}"
                            class="form-control {% if form.username.errors %}is-invalid{% endif %}" required>
                            {% if form.username.help_text %}
                                <small class="form-text text-muted help-text-custom">{{ form.username.help_text|safe }}</small> <!-- ←
                    ヘルプテキスト！ -->
                            {% endif %}
                            {% for error in form.username.errors %}
                                <div class="invalid-feedback d-block">{{ error }}</div>
                            {% endfor %}
                        </div>

                    <!-- Password (最初の入力) フィールド -->
                    <!-- UserCreationFormでは、これは password1 という名前のはず (ビューで使ってるフォームを確認してね！)
                もし views.py で SignUpView(form_class=UserCreationForm) ってなってるなら、
                form.password1 と form.password2 でアクセスできるはず！ -->
                        <div class="mb-3">
                            <label for="{{ form.password1.id_for_label }}" class="form-label">{{ form.password1.label }}</label> <!-- ← password1 に直すかも？ -->
                            <input type="password" name="{{ form.password1.name }}" id="{{ form.password1.id_for_label }}"
                            class="form-control {% if form.password1.errors %}is-invalid{% endif %}" required>
                        <!-- ← password1 に直すかも？ -->
                            {% if form.password1.help_text %} <!-- ← password1 に直すかも？ -->
                        <!-- ↓↓↓ ヘルプテキストの表示方法、箇条書き風にするならこう ↓↓↓ -->
                                <small class="form-text text-muted help-text-custom">
                                    {{ form.password1.help_text|safe }}
                                </small>
                            {% endif %}
                            {% for error in form.password1.errors %} <!-- ← password1 に直すかも？ -->
                                <div class="invalid-feedback d-block">{{ error }}</div>
                            {% endfor %}
                        </div>

                    <!-- --- Password (確認用) フィールド -->
                    <!-- UserCreationFormでは、これは password2 という名前のはず -->
                        <div class="mb-3">
                            <label for="{{ form.password2.id_for_label }}" class="form-label">{{ form.password2.label }}</label> <!-- ← password2 に直すかも？ -->
                            <input type="password" name="{{ form.password2.name }}" id="{{ form.password2.id_for_label }}"
                            class="form-control {% if form.password2.errors %}is-invalid{% endif %}" required>
                        <!-- ← password2 に直すかも？ -->
                            {% if form.password2.help_text %} <!-- ← password2 に直すかも？ -->
                                <small class="form-text text-muted help-text-custom">{{ form.password2.help_text|safe }}</small> <!--
                    ← ヘルプテキスト！ -->
                            {% endif %}
                            {% for error in form.password2.errors %} <!-- ← password2 に直すかも？ -->
                                <div class="invalid-feedback d-block">{{ error }}</div>
                            {% endfor %}
                        </div>
                    <!-- ↑↑↑ ★★★ ここまでが個別レンダリング ★★★ ↑↑↑ -->

                    <!-- ↓↓↓ ボタンのd-gridのスペルミス修正！ (d-gird gep-2 -> d-grid gap-2) ↓↓↓ -->
                        <div class="d-grid gap-2 mt-4"> <!-- フォームとボタンの間に少し余白 (mt-3 から mt-4 へ) -->
                            <button type="submit" class="btn btn-primary btn-lg">登録する</button> <!-- btn-lgで少し大きく -->
                        </div>
                    </form>

                    <p class="mt-3 text-center"> <!-- ここは変更なしでもOK！ -->
                        すでにアカウントをお持ちですか？ <a href="{% url 'accounts:login' %}">ログインはこちら</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
--------------------------------------------------
File: \accounts\admin.py
from django.contrib import admin

# Register your models here.

--------------------------------------------------
File: \accounts\apps.py
from django.apps import AppConfig


class AccountsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'accounts'

--------------------------------------------------
File: \accounts\models.py
from django.db import models

# Create your models here.

--------------------------------------------------
File: \accounts\tests.py
from django.test import TestCase

# Create your tests here.

--------------------------------------------------
File: \accounts\urls.py
from django.urls import path
from . import views  # viewsモジュールをインポート

app_name ="accounts"

# 5/12---includeを追加
# アカウントアプリにURLパイ引っ越し

urlpatterns = [
    path("login/", views.MyLoginView.as_view(), name="login"),
    path("logout/", views.MyLogoutView.as_view(), name="logout"),
    path("signup/", views.SignUpView.as_view(), name="signup"),
    path("password_change/", views.MyPasswordChangeView.as_view(), name="password_change"),
    path("password_change/done/", views.MyPasswordChangeDoneView.as_view(), name="password_change_done"),
    path("delete/", views.AccountDeleteView.as_view(), name="account_delete"),
    path('delete/complete/', views.AccountDeleteCompleteView.as_view(), name='account_delete_complete'),
]
# 後ほど新規登録などのURLも追加していく
# 5/14---サインアップパス追加
# ブラウザで /accounts/signup/ っていうURLにアクセスすると、さっき作った SignUpView が動いて、signup.html が表示されるようになる
# 5/19---削除処理対応のURL追記
--------------------------------------------------
File: \accounts\views.py
from django.shortcuts import render, redirect
from django.contrib.auth import views as auth_views
from django.urls import reverse_lazy
from django.views import generic
from django.contrib.auth.forms import UserCreationForm, PasswordChangeForm
from django.contrib.auth.views import PasswordChangeView
from django.contrib.auth.forms import AuthenticationForm
from django.contrib.auth import authenticate
from django.views import View
from django.contrib.auth.decorators import login_required
from django.utils.decorators import method_decorator
from django.conf import settings
from django.contrib.auth import logout
from django.contrib.auth.views import LoginView
from django.views.generic import TemplateView

# 5/13---reverse_lazy、generic、UserCreationForm、追加
# 5/17---PasswordChangeViewとPasswordChangeForm追加
# 5/19---AuthenticationFormとauthenticateとView追記、アカウント削除用
# 5/21---redirectとsettingsインポート追記
# 5/21---import logout追加
# 5/26---LoginView追記

# 新規登録
class SignUpView(generic.CreateView):
    form_class = UserCreationForm
    success_url = reverse_lazy("accounts:login")
    # success_url = "/accounts/login/"
    template_name = "accounts/signup.html"

# 上記は上から使うフォームの指定、登録成功したらログインページに飛ばす、表示するHTMLテンプレートを指定。

    def dispatch(self, request, *args, **kwargs):
        if request.user.is_authenticated:
            # ユーザーがログイン認証済みの場合
            # settings.LOGIN_REDIRECT_URL にリダイレクトする
            # LOGIN_REDIRECT_URL が設定されていなければ、サイトのルート ('/') にリダイレクト
            # (もし LOGIN_REDIRECT_URL が未設定で、ルートがログインページの場合、
            #  別の適切なログイン後ページURLを直接指定するのもアリ)    
            redirect_url = getattr(settings, "LOGIN_REDIRECT_URL", "/")
            if redirect_url == "/" and not request.user.has_perm(" some_permission_that_indicates_logged_in_area"):
                # 例: ルートがログインページで、まだチャート選択画面のURLが決まってない場合など、
                #     とりあえず '/admin/' など、確実に存在する場所に飛ばすのも手
                # redirect_url = '/admin/' # (これはあくまで例！)
                # もしくは、albatross_app のトップページのURL名を指定する (例: 'albatross_app:chart_list')
                pass # ここはマサ君のアプリの構成に合わせて調整してね！★チャート画面に飛ばす処理をあとで書く。
            
            return redirect(redirect_url)
        # ユーザーがログイン認証されていなければ、通常の新規登録処理を続行
        return super().dispatch(request, *args, **kwargs)

"""
上記に入る可能性のあるコード
        try:
            redirect_url = reverse_lazy('albatross_app:chart_top') # チャートのトップ画面へ
        except NoReverseMatch: # もし 'albatross_app:chart_top' がまだ無かったらフォールバック
            redirect_url = getattr(settings, 'LOGIN_REDIRECT_URL', '/')
        return redirect(redirect_url)
"""


# ログイン処理
# 5/13---下記accounts/login.htmlのアカウント部分を_appからアカウントに修正
# 5/14---スペルミス修正

class MyLoginView(LoginView):
    template_name = "accounts/login.html"
    # success_url は settings.LOGIN_REDIRECT_URL が使われる

    def dispatch(self, request, *args, **kwargs):
        if request.user.is_authenticated:
            # 既にログイン済みの場合は、LOGIN_REDIRECT_URL にリダイレクト
            return redirect(settings.LOGIN_REDIRECT_URL or "/")# or '/' は念のため
        return super().dispatch(request, *args, **kwargs)



# ログアウト処理
class MyLogoutView(auth_views.LogoutView):
    pass
# ログアウト後のリダイレクト先は settings.py の LOGOUT_REDIRECT_URL で指定するのがおすすめ
# (例: LOGOUT_REDIRECT_URL = '/login/')


# パスワード変更（更新）処理
class MyPasswordChangeView(PasswordChangeView):
    template_name = "accounts/password_change_form.html"
    success_url = reverse_lazy("accounts:password_change_done")

# MyPasswordChangeView っていう名前で、さっきインポートした PasswordChangeView をカッコ () の中に書いて「継承」してる。
# PasswordChangeView が持ってる便利な機能（パスワード変更処理とか、フォームの取り扱いとか）を、MyPasswordChangeView も使えるようになる


# パスワード変更完了のためのページの処理
class MyPasswordChangeDoneView(generic.TemplateView):
    template_name = "accounts/password_change_done.html"


# 5/19---削除系追記していく
# アカウント削除ビュー

@method_decorator(login_required, name="dispatch")
class AccountDeleteView(View):
    template_name = "accounts/account_delete_form.html"
    # success_url = reverse_lazy("accounts:login")
    # 削除再工事のリダイレクト先（今回はJSで制御かも）

    def get(self, request,*args, **kwargs):
        # GETリクエスト時 (最初にページを開いた時)
        # 空の認証フォームをテンプレートに渡す (パスワード入力欄のため)
        print("★★★ AccountDeleteView GET method CALLED! ★★★") # ← まずこれが呼ばれるか？
        print(f"★★★ User authenticated in GET: {request.user.is_authenticated} ★★★") # ← ログイン状態は？
        print(f"★★★ Session user_deleted_flag in GET: {request.session.get('user_deleted_and_needs_logout_flag')} ★★★") # ← セッションフラグは？
        print(f"★★★ Session show_modal_flag in GET: {request.session.get('show_delete_modal_on_load_flag')} ★★★") # ← セッションフラグは？

        form = AuthenticationForm()
        show_deleted_message_and_logout = request.session.pop('user_deleted_and_needs_logout_flag', False)
        # セッションから「モーダル自動表示」フラグを取得 (もしパスワード認証成功直後なら)
        show_modal_on_load = request.session.pop('show_delete_modal_on_load_flag', False)
        context = {
            "form": form,
            # "user_deleted_and_needs_logout": show_deleted_message_and_logout,
            "show_delete_modal_on_load": show_modal_on_load, # モーダル表示用フラグも渡す
        }
        print(f"★★★ GET時 context: {context}") # デバッグログ
        return render(request, self.template_name, context)
    
    def post(self, request, *args, **kwargs):
        context = {}
        print("--- AccountDeleteView POSTメソッド開始 ---") # ★デバッグログ
        # POSTリクエスト時 (ユーザーがフォームを送信した時)
        # ここでパスワード認証と、最終確認後の削除処理を行う
        if "execute_delete" in request.POST and request.POST.get("execute_delete") == "true":
            print(">>> execute_delete リクエストを受け取りました") # ★デバッグログ
            # 最終確認後の実行フェーズ
            user_to_delete = request.user

            if user_to_delete.is_authenticated: # 念のためログインしてるか確認
                user_to_delete.is_active = False
                user_to_delete.save()
                # logout(request) # ここでログアウトしちゃう！
                
                print(f"★★★ ユーザー {user_to_delete.username} の is_active を False にしました。")
                request.session['account_successfully_deleted_flag'] = True # 新しいセッションフラグ名
                return redirect('accounts:account_delete_complete') # ← 同じページにリダイレクトして、GETでフラグを読み込む
            else:
                print("★★★ 削除実行フェーズ: ユーザーが認証されていません (通常ありえない)")
                # 何らかのエラー処理 (ログインページへリダイレクトなど)
                return redirect('accounts:login')


        else:
            print(">>> パスワード認証フェーズ")
            # パスワード認証フェーズ
            form_data = request.POST.copy()
            form_data["username"] = request.user.username
            form = AuthenticationForm(request, data=form_data)


        if form.is_valid():
            # パスワード認証OK！
            print("★★★ パスワード認証成功！ ★★★") # デバッグ用
            # ここで、JavaScriptに「認証OKだよ」って伝えるか、
            # もしくは、特定のフラグを立てて再度同じテンプレートを表示し、
            # テンプレート側でJavaScriptのconfirmを出す、みたいな流れになるかな。
            request.session['show_delete_modal_on_load_flag'] = True
            return redirect('accounts:account_delete') # 同じページにリダイレクトしてGETでモーダル表示
        else:
                print("★★★ パスワード認証失敗… ★★★")
                context["form"] = form # エラーメッセージ付きのフォーム
                # ↓↓↓ ★変更点５：エラー時も context をちゃんと渡してrender ★↓↓↓
                return render(request, self.template_name, context)

            # 今回は、まず「パスワード認証が通った」ということを確認したいので、
            # 一旦、コンソールにメッセージを出すだけにしてみようか！

            # TODO: ここに最終確認ポップアップ表示のトリガーと、実際の削除処理を書く！
            # (Ajax使うか、もう一度POSTさせるか、など設計による)

            # 仮に、認証成功したら、メッセージ付きで同じページを再表示する例
            # (実際には、ここでJSを動かすための工夫が必要になる)

@method_decorator(login_required, name="dispatch")
class AccountDeleteCompleteView(View): # ← ★★★ このクラスがちゃんと存在してるか？ ★★★
    template_name = "accounts/account_delete_complete.html"

    def get(self, request, *args, **kwargs):
        print("★★★ AccountDeleteCompleteView GET method CALLED! ★★★")
        account_successfully_deleted = request.session.pop('account_successfully_deleted_flag', False)
        if not account_successfully_deleted:
            print(">>> 削除完了フラグなしでアクセスされました。リダイレクトします。")
            return redirect('accounts:login')

        context = {
            'user_deleted_and_needs_logout': True,
        }
        print(f"★★★ 削除完了ページ表示: context = {context}")
        return render(request, self.template_name, context)
--------------------------------------------------
File: \accounts\__init__.py
--------------------------------------------------
File: \albatross\asgi.py
"""
ASGI config for albatross project.

It exposes the ASGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/4.2/howto/deployment/asgi/
"""

import os

from django.core.asgi import get_asgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'albatross.settings')

application = get_asgi_application()

--------------------------------------------------
File: \albatross\settings.py
"""
Django settings for albatross project.

Generated by 'django-admin startproject' using Django 4.2.2.

For more information on this file, see
https://docs.djangoproject.com/en/4.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/4.2/ref/settings/
"""

from pathlib import Path
import os
from django.urls import reverse_lazy


# 5/9---osモジュールを使うためにosをインポート追記
# 5/22---reverse_lazy追加



# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-ez#l2(hhk9bx9cm*r9nvt8-#&2@7_=-ulpnzvs24jjq*6vjxv9'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = ['localhost', '127.0.0.1']


# Application definition

# 5/9...アロードホストを空欄から上記に変更「今まさに使ってる、この自分のパソコン」 を指す特別な名前と住所



INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    "accounts",
    "logs",
    "albatross_app",
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'albatross.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'albatross.wsgi.application'


# Database
# https://docs.djangoproject.com/en/4.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}


# Password validation
# https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/4.2/topics/i18n/

LANGUAGE_CODE = 'ja'

TIME_ZONE = 'Asia/Tokyo'

USE_I18N = True

USE_TZ = True

# 5/9...言語は日本語に変更
# 5/9...タイムゾーンはアジア東京に変更


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/4.2/howto/static-files/

STATIC_URL = 'static/'

# Default primary key field type
# https://docs.djangoproject.com/en/4.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'static'),
]


# 5/9...Djangoプロジェクト内のあちこちにある静的ファイルを、一箇所に集めるためのフォルダの場所を指定する


LOGIN_URL = "accounts:login"
LOGIN_REDIRECT_URL = "albatross_app:chart_type_list"
LOGOUT_REDIRECT_URL = reverse_lazy("accounts:login")

# 1 これは urls.py で name='login' って付けた名前を指定
# 2 ログイン成功したらトップページ / に飛ばすよ、って意味。
# 2 もしログイン後にチャート選択画面とかに直接飛ばしたいなら、そのURLに変える！
# 3 ログアウトしたら、name='login' のページ（つまりログインページ）に飛ばすよ、って意味。
# "/"からreverse_lazy("accounts:login")に変更、明示的に飛ばす。
# ↑↑↑ 'albatross_app' (アプリ名) と 'chart_type_list' (さっき付けたURL名) を指定！

--------------------------------------------------
File: \albatross\urls.py
from django.urls import path, include
from django.contrib import admin
from accounts import views as accounts_views

# from . import viewsでviews.py を読み込み
# テンプレートとかでURLを指定する時に {% url 'albatross_app:login' %} みたいに書けるようにするための設定(ショートカット的な)
# インクルード追加
# app_name = "albatross_app"左記削除
# ここは全体の管理のため上記の特定のアプリの名前は書かない、指定しない
# from accounts import views as accounts_views のインポート追記



urlpatterns = [
    path('admin/', admin.site.urls),
    # path("app/", include("albatross_app.urls")),
    path("accounts/", include("accounts.urls")),
    path("", accounts_views.MyLoginView.as_view(), name="top_login"),
    path("app/", include("albatross_app.urls", namespace="albatross_app")),
]
# path2行目のappはurlに表示される部分のためアンスコは入れずにスッキリさせる
# pathのあとの（）内で第一引数がURL表示部分、第二引数が「どのアプリの urls.py ファイルを読み込んでね！」っていう、Pythonモジュールへのパス
# 5/14---このパス("app/", include("albatross_app.urls")),は一時的にコメントアウト
# 上記はランサーバーでエラーになる原因と思われるため一時的にコメントアウト
# サイトのルート('/') にアクセスが来たら、accountsアプリのMyLoginViewを直接呼び出す！
--------------------------------------------------
File: \albatross\wsgi.py
"""
WSGI config for albatross project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/4.2/howto/deployment/wsgi/
"""

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'albatross.settings')

application = get_wsgi_application()

--------------------------------------------------
File: \albatross\__init__.py
--------------------------------------------------
File: \albatross_app\migrations\0001_initial.py
# Generated by Django 4.2.2 on 2025-05-22 11:04

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    initial = True

    dependencies = [
    ]

    operations = [
        migrations.CreateModel(
            name='ChartStep',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('quewtion_text', models.TextField(verbose_name='質問文')),
                ('is_starting_step', models.BooleanField(default=False, verbose_name='開始ステップか')),
                ('is_final_step', models.BooleanField(default=False, verbose_name='最終ステップか')),
                ('solution_text', models.TextField(blank=True, null=True, verbose_name='解決策/メッセージ')),
                ('no_next_step', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='goes_to_no', to='albatross_app.chartstep', verbose_name='「いいえ」の場合の次のステップ')),
                ('yes_next_step', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='goes_to_yes', to='albatross_app.chartstep', verbose_name='「はい」次のステップ')),
            ],
            options={
                'verbose_name': 'チャートステップ',
                'verbose_name_plural': 'チャートステップ',
            },
        ),
    ]

--------------------------------------------------
File: \albatross_app\migrations\0002_charttype.py
# Generated by Django 4.2.2 on 2025-05-22 11:47

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('albatross_app', '0001_initial'),
    ]

    operations = [
        migrations.CreateModel(
            name='ChartType',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('name', models.CharField(max_length=100, unique=True, verbose_name='チャート種別名')),
                ('descriotion', models.TextField(blank=True, verbose_name='チャート種別の説明')),
            ],
            options={
                'verbose_name': 'チャート種別',
                'verbose_name_plural': 'チャート種別',
            },
        ),
    ]

--------------------------------------------------
File: \albatross_app\migrations\0003_rename_quewtion_text_chartstep_question_text.py
# Generated by Django 4.2.2 on 2025-05-22 12:22

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('albatross_app', '0002_charttype'),
    ]

    operations = [
        migrations.RenameField(
            model_name='chartstep',
            old_name='quewtion_text',
            new_name='question_text',
        ),
    ]

--------------------------------------------------
File: \albatross_app\migrations\0004_chartstep_chart_type.py
# Generated by Django 4.2.2 on 2025-05-26 11:45

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    dependencies = [
        ('albatross_app', '0003_rename_quewtion_text_chartstep_question_text'),
    ]

    operations = [
        migrations.AddField(
            model_name='chartstep',
            name='chart_type',
            field=models.ForeignKey(default=1, on_delete=django.db.models.deletion.CASCADE, to='albatross_app.charttype', verbose_name='チャート種別'),
            preserve_default=False,
        ),
    ]

--------------------------------------------------
File: \albatross_app\migrations\0005_troubleshootingsession_sessionlog.py
# Generated by Django 4.2.2 on 2025-05-28 07:25

from django.conf import settings
from django.db import migrations, models
import django.db.models.deletion
import django.utils.timezone


class Migration(migrations.Migration):

    dependencies = [
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
        ('albatross_app', '0004_chartstep_chart_type'),
    ]

    operations = [
        migrations.CreateModel(
            name='TroubleshootingSession',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('end_time', models.DateTimeField(default=django.utils.timezone.now, verbose_name='完了日時')),
                ('chart_type', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, to='albatross_app.charttype', verbose_name='チャート種別')),
                ('user', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, to=settings.AUTH_USER_MODEL, verbose_name='ユーザー')),
            ],
            options={
                'verbose_name': 'トラブルシューティングセッション',
                'verbose_name_plural': 'トラブルシューティングセッション',
                'ordering': ['-end_time'],
            },
        ),
        migrations.CreateModel(
            name='SessionLog',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('answer', models.CharField(choices=[('yes', 'はい'), ('no', 'いいえ'), ('unknown', '不明')], max_length=10, verbose_name='回答')),
                ('chart_step', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, to='albatross_app.chartstep', verbose_name='チャートステップ')),
                ('session', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='logs', to='albatross_app.troubleshootingsession', verbose_name='セッション')),
            ],
            options={
                'verbose_name': 'セッションログ',
                'verbose_name_plural': 'セッションログ',
                'unique_together': {('session', 'chart_step')},
            },
        ),
    ]

--------------------------------------------------
File: \albatross_app\migrations\0006_rename_descriotion_charttype_description_and_more.py
# Generated by Django 4.2.2 on 2025-06-04 10:12

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('albatross_app', '0005_troubleshootingsession_sessionlog'),
    ]

    operations = [
        migrations.RenameField(
            model_name='charttype',
            old_name='descriotion',
            new_name='description',
        ),
        migrations.AddField(
            model_name='chartstep',
            name='solution_type',
            field=models.CharField(blank=True, choices=[('improved', '解決/改善'), ('unresolved', '未解決/改善なし'), ('info', '情報/その他')], default=None, max_length=20, null=True, verbose_name='解決タイプ'),
        ),
    ]

--------------------------------------------------
File: \albatross_app\migrations\__init__.py
--------------------------------------------------
File: \albatross_app\static\albatross_app\css\albatross_app_chart.css
/* albatross_app/static/albatross_app/css/chart_display_styles.css */

/* =============================================== */
/* Chart Display Page Styles (チャート表示画面のスタイル) */
/* =============================================== */

/* --- 回答済みの質問ステップのスタイル --- */
.chart-step.answered-step {
    opacity: 0.6;
    transition: opacity 0.3s ease-in-out;
}

/* --- 選択されたボタンのスタイル --- */
.btn.selected-button {
    background-color: #0d6efd; /* 例：青色 */
    border-color: #0a58ca;
    color: white !important;
}

/* 改善が見つかった最終ステップのスタイル */
.chart-step.final-step-improved { /* またはカード全体 .final-step-improved に直接 */
    border: 3px solid #198754 !important; /* 緑色の太枠 */
    /* (他のスタイル: 背景色とか、角丸とか、お好みで) */
}

/* 解決しない最終ステップのスタイル (デフォルトで適用されるイメージ) */
.chart-step.final-step-unresolved { /* またはカード全体 .final-step-unresolved に直接 */
    border: 3px solid #dc3545 !important; /* 赤色の太枠 */
    /* (他のスタイル) */
}


--------------------------------------------------
File: \albatross_app\static\albatross_app\css\common_styles.css
/* albatross_app/static/albatross_app/css/common_styles.css */
button, input, select, textarea {
    font-size: inherit !important;
}
/* 他にも、アプリ全体で共通して使いたいスタイルがあれば、ここに追加していく！ */
--------------------------------------------------
File: \albatross_app\static\albatross_app\css\top_styles.css
/* ログインページ専用のスタイル */
.login-page-container {
    min-height: calc(100vh - 56px - 56px); /* 画面の高さ - ヘッダーの高さ - フッターの高さ (もしあれば) */
    /* ↑ 56pxはBootstrapのnavbarのデフォルトの高さの目安。実際の高さに合わせて調整してね！ */
    /* もしフッターがなければ、calc(100vh - 56px) とか、もっとシンプルに 80vh とかでもOK */
    background-color: #e9ecef; /* 背景を少しグレーにする例 (お好みで) */
}

.login-form-card {
    background-color: #ffffff; /* カードの背景を白に */
    border-radius: 0.75rem;    /* 角を丸く */
    box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.1); /* 影を付ける */
}

/* {{ form.as_p }} を使う場合の入力欄の調整 (もし入力欄が幅いっぱいになってなかったら) */
.login-form-card form p input[type="text"],
.login-form-card form p input[type="password"] {
    width: 100%;             /* 入力欄を親要素の幅いっぱいに広げる */
    /* padding: 0.75rem;        Bootstrapの.form-controlが適用されてれば不要かも */
    /* border: 1px solid #ced4da; Bootstrapの.form-controlが適用されてれば不要かも */
    /* border-radius: 0.25rem;  Bootstrapの.form-controlが適用されてれば不要かも */
    box-sizing: border-box;    /* paddingとborderをwidthに含める */
}
/* もし form.as_p で出力される <p> タグの余白が気になるなら */
.login-form-card form p {
    margin-bottom: 1rem; /* 例えばこれくらいに調整 */
}
.login-form-card form p label {
    margin-bottom: 0.25rem; /* ラベルの下の余白を少し詰める例 */
    font-weight: 500; /* ラベルの文字を少し太くする例 */
}


/* ログインボタン (d-gridで横幅いっぱいになってるはずだけど、念のため) */
.login-form-card .d-grid .btn {
    font-weight: bold;
    /* ここでさらにボタンの見た目を調整できるよ */
    /* 例: font-weight: bold; */
    /* 例: letter-spacing: 0.5px; */
}


--------------------------------------------------
File: \albatross_app\templates\albatross_app\base.html
<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}アルバトロス{% endblock %}</title> <!-- 各ページで上書きできるようblockタグで囲う -->

    <!-- ブートストラップ等共通で使うCSSを読み込む場所 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- プロジェクト共通のファイルがあればここで読み込む -->
    {% load static %}<!-- staticファイルを使うためのコード-->
    <!-- <link rel="stylesheet" href="{% static 'css/base.css' %}"> -->
    <!-- static/css/base.cssを読み込む場合 -->
    <link rel="stylesheet" href="{% static 'accounts/css/accounts_styles.css' %}">
    <link rel="stylesheet" href="{% static 'albatross_app/css/albatross_app_chart.css' %}">
    <link rel="stylesheet" href="{% static 'albatross_app/css/common_styles.css' %}">

    {% block extra_head %}{% endblock %}
    <!-- 各ページで追加のcssやHEADタグを入れたいとき用 -->


</head>


<!-- 5/14ヘッダー中身追記 -->

<body>

    <header>
        <nav class="navbar navbar-expand-lg navber-light bg-light"> <!-- ここからナビゲーションバー -->
            <div class="container-fluid">
                <a class="navbar-brand fw-light text-primary fs-4"
                    href="{% if user.is_authenticated %}{% url 'albatross_app:chart_type_list' %}{% else %}{% url 'accounts:login' %}{% endif %}">
                    <!-- 以下画像の場所 -->
                    <img src="{% static 'albatross_app/images/flap.png' %}" alt="albatrossロゴ" width="40" height="40"
                        class="d-inline-block align-text-top me-2">
                    <!-- ↑↑↑ srcは仮のパス、altは代替テキスト、width/heightで仮のサイズ、classで見た目調整 -->
                    Albatross
                </a>
                <div class="ms-auto">
                    {% if user.is_authenticated %}
                    <ul class="navbar-nav flex-row">
                        <li class="nav-item me-2">
                            <button type="button" class="btn btn-outline-secondary btn-sm" data-bs-toggle="modal"
                                data-bs-target="#screenSettingsModal">
                                <i class="bi bi-gear-fill"></i>
                                画面設定
                            </button>
                        </li>

                        <li class="nav-item me-2">
                            <div class="btn-group">
                                <button type="button" class="btn btn-outline-secondary btn-sm dropdown-toggle"
                                    data-bs-toggle="dropdown" aria-expanded="false">
                                    アカウント設定
                                </button>

                                <ul class="dropdown-menu dropdown-menu-end">
                                    {% if request.resolver_match.url_name == "password_change" or request.resolver_match.url_name == 'account_delete' %}
                                        <li><a class="dropdown-item" href="{% url 'accounts:password_change' %}">パスワード変更</a></li>
                                    {% else %}
                                        <li><a class="dropdown-item" href="{% url 'accounts:password_change' %}" target="_blank">パスワード変更</a></li>
                                    {% endif %}

                                    <li><hr class="dropdown-divider"></li> {# ← ★★★ここを修正！★★★ #}

                                    {% if request.resolver_match.url_name == "account_delete" or request.resolver_match.url_name == "password_change" %}
                                        <li><a class="dropdown-item" href="{% url 'accounts:account_delete'%}">アカウント削除</a></li>
                                    {% else %}
                                        <li><a class="dropdown-item" href="{% url 'accounts:account_delete'%}" target="_blank">アカウント削除</a></li>
                                    {% endif %}
                                </ul>

                            </div>
                        </li>
                        <li class="nav-item">
                            <form action="{% url 'accounts:logout'%}" method="post" class="d-inline">

                                {% csrf_token %}
                                <button type="submit" class="btn btn-outline-secondary btn-sm">
                                    ログアウト
                                </button>
                            </form>
                        </li>
                    </ul>
                    {% else %}
                    <!-- 未ログインユーザー向けの表示 -->
                    <ul class="navbar-nav flex-row">
                        <li class="nav-item">
                            <a href="{% url 'accounts:login' %}" class="btn btn-outline-primary btn-sm">ログイン</a>
                        </li>
                        <li class="nav-item ms-2">
                            <a href="{% url 'accounts:signup' %}" class="btn btn-primary btn-sm ms-2">新規登録</a>
                        </li>
                    </ul>
                    <!-- この「新規登録」ボタンは、user.is_authenticated が False の時だけ表示される -->
                    {% endif %}
                </div>
            </div>
        </nav>
    </header>

    <main class="container mt-4"><!-- メインコンテンツ表示 -->
        {% block content %}
        <!-- ここに各ページの中身が入る -->
        {% endblock %}
    </main>

    <footer class="mt-auto py-3 bg-light text-center">
        <div class="container">
            <span class="text-muted">© 2025 アルバトロス</span>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- ↑ Popper.js も一緒に入ってるバンドル版を使うと楽ちん！ -->

    {% block extra_js %}
<script>
document.addEventListener('DOMContentLoaded', function() {
    // 文字サイズ変更ボタンの要素を取得
    const btnSmall = document.getElementById('fontSizeSmall');
    const btnDefault = document.getElementById('fontSizeDefault');
    const btnLarge = document.getElementById('fontSizeLarge');
    const btnsuperLarge = document.getElementById('fontSizesuperLarge');

    // ページ全体の文字サイズを変更する対象の要素 (bodyタグとか、特定のコンテナとか)
    const bodyElement = document.body; // body全体の文字サイズを変える例

    const savedFontSizePreference = localStorage.getItem('userFontSizePreference');

    if (savedFontSizePreference) {
        console.log('localStorageから文字サイズ設定を読み込みました:', savedFontSizePreference);
        let fontSizeToApply = '';
        let activeButtonId = 'fontSizeDefault'; // デフォルトでアクティブにするボタンのID

        if (savedFontSizePreference === 'small') {
            fontSizeToApply = '0.8rem';
            activeButtonId = 'fontSizeSmall';
        } else if (savedFontSizePreference === 'default') {
            fontSizeToApply = ''; // または '1rem'
            activeButtonId = 'fontSizeDefault';
        } else if (savedFontSizePreference === 'large') {
            fontSizeToApply = '1.2rem';
            activeButtonId = 'fontSizeLarge';
        } else if (savedFontSizePreference === 'super-large') {
            fontSizeToApply = '1.5rem';
            activeButtonId = 'fontSizeSuperLarge'; // 「極大」ボタンのIDも必要になるね！
        }

        bodyElement.style.fontSize = fontSizeToApply;

        // 対応するボタンをアクティブ表示にする
        const buttons = { // ボタンのIDと要素をマッピングしておくと便利
            'fontSizeSmall': document.getElementById('fontSizeSmall'),
            'fontSizeDefault': document.getElementById('fontSizeDefault'),
            'fontSizeLarge': document.getElementById('fontSizeLarge'),
            'fontSizeSuperLarge': document.getElementById('fontSizeSuperLarge') // 「極大」ボタンの要素も取得
        };
        // updateActiveButton 関数が使えるように、ここで定義しておくか、
        // もしくは、ここで直接アクティブクラスを付け外しする処理を書く
        if (buttons[activeButtonId]) {
             // まず全てのボタンからactiveを外す (updateActiveButton関数と同じような処理)
            for (const id in buttons) {
                if (buttons[id]) buttons[id].classList.remove('active');
            }
            buttons[activeButtonId].classList.add('active'); // 保存されてた設定に対応するボタンをアクティブに
            console.log(activeButtonId + ' ボタンをアクティブにしました。');
        }
    }

    // 各ボタンにクリックイベントリスナーを設定
    if (btnSmall) {
        btnSmall.addEventListener('click', function() {
            bodyElement.style.fontSize = '0.8rem'; // 小さい文字サイズ (値はお好みで)
            updateActiveButton(this); // 押されたボタンをアクティブにする
            localStorage.setItem('userFontSizePreference', 'small');
            console.log('文字サイズを「小」に変更しました。');
        });
    }

    if (btnDefault) {
        btnDefault.addEventListener('click', function() {
            bodyElement.style.fontSize = ''; // 標準サイズ (CSSで設定されてるデフォルトに戻す)
            // または bodyElement.style.fontSize = '1rem'; みたいに明示的に指定してもOK
            updateActiveButton(this);
            localStorage.setItem('userFontSizePreference', 'default');
            console.log('文字サイズを「標準」に変更しました。');
        });
    }

    if (btnLarge) {
        btnLarge.addEventListener('click', function() {
            bodyElement.style.fontSize = '1.2rem'; // 大きい文字サイズ (値はお好みで)
            updateActiveButton(this);
            localStorage.setItem('userFontSizePreference', 'large');
            console.log('文字サイズを「大」に変更しました。');
        });
    }

    if (btnsuperLarge) {
    btnsuperLarge.addEventListener('click', function() {
        bodyElement.style.fontSize = '1.6rem'; // 大きい文字サイズ (値はお好みで)
        updateActiveButton(this);
        localStorage.setItem('userFontSizePreference', 'super-large');
        console.log('文字サイズを「極大」に変更しました。');
    });
}

    // 押されたボタンに 'active' クラスを付けて、他からは外すヘルパー関数
    function updateActiveButton(activeButton) {
        // 全ての文字サイズボタンから 'active' クラスを一旦削除
        [btnSmall, btnDefault, btnLarge, btnsuperLarge].forEach(function(btn) {
            if (btn) btn.classList.remove('active');
        });
        // 押されたボタンに 'active' クラスを追加
        if (activeButton) {
            activeButton.classList.add('active');
        }
    }

    // (おまけ) モーダルが表示された時に、現在の文字サイズに合わせてボタンのアクティブ状態を更新する (もしやるなら)
    const screenSettingsModal = document.getElementById('screenSettingsModal');
    if (screenSettingsModal) {
        screenSettingsModal.addEventListener('shown.bs.modal', function () {
            const currentFontSize = window.getComputedStyle(bodyElement).fontSize;
            // console.log('現在の計算済みフォントサイズ:', currentFontSize);
            // ここで currentFontSize の値に応じて、適切なボタンに active クラスを付け直す処理を書ける
            // (例: '12.8px' だったら btnSmall をアクティブに、とか。ちょっと面倒かもだけど)
            // 今回は、まずは押したボタンがアクティブになるだけで十分かな！
        });
    }

});
</script>
{% endblock %}

    <!-- Screen Settings Modal -->
<div class="modal fade" id="screenSettingsModal" tabindex="-1" aria-labelledby="screenSettingsModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered"> {# ← modal-dialog-centered で上下中央寄せ！ #}
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="screenSettingsModalLabel">画面設定</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p class="mb-2">文字サイズを選択してください：</p>
                <div class="btn-group" role="group" aria-label="文字サイズ選択">
                    <button type="button" class="btn btn-outline-primary" id="fontSizeSmall">小</button>
                    <button type="button" class="btn btn-outline-primary active" id="fontSizeDefault">標準</button> {# ← 最初は「標準」が選択されてる感じ #}
                    <button type="button" class="btn btn-outline-primary" id="fontSizeLarge">大</button>
                    <button type="button" class="btn btn-outline-primary" id="fontSizesuperLarge">極大</button>
                </div>
                <p>ここに文字サイズ変更のボタンとかが入る予定だよ！</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">閉じる</button>
                {# <button type="button" class="btn btn-primary">変更を保存</button> ← 保存ボタンは、ここでは不要かな？即時反映で #}
            </div>
        </div>
    </div>
</div>
<!-- End Screen Settings Modal -->

</body>

</html>
--------------------------------------------------
File: \albatross_app\templates\albatross_app\chart_display.html
{% extends "albatross_app/base.html" %}
{% load static %}

{% block title %}診断チャート{% endblock %}

{% block extra_head %}
<link rel="stylesheet" href="{% static 'albatross_app/css/chart_styles.css' %}">
{% endblock %}


{% block content %}
<div class="container mt-3">

    <p class="text-end">こんにちは、{{ user.username }} さん！</p>

    <h2 class="mb-4 text-center">診断チャート：{{ chart_type.name }}</h2>

    {% if chart_steps %}
    <div id="chart-area"><!-- ← JavaScriptで操作するためのIDを付けとく (任意) -->
        {% for step in chart_steps %}
        <div class="card mb-5 chart-step 
        {% if step.is_final_step %}final-step-card{% endif %}"
        data-step-id="{{ step.id }}" 
        style="display: block;"
        {% if step.is_final_step %}
            data-is-final-step="true"
            data-solution-type="{{ step.solution_type_for_js | default:'unknown' }}"
            {% endif %}>
            <!-- ↑↑↑各ステップをカードで囲む (style="display: block;" はJSで制御する前の仮表示) -->
            
            <div class="card-header">ステップ {{ forloop.counter }}
                <!-- ↑↑↑何番目のステップか表示 (forloop.counter は1から始まる) -->

                <!-- {% if step.is_starting_step %}
                <span class="badge bg-primary">開始ステップ</span>
                {% endif %} -->
                {% if step.is_final_step %}
                <span class="badge bg-success">最終ステップ</span>
                {% endif %}
            </div>
            <div class="card-body">
                <p class="card-text">{{ step.question_text }}</p>

                {% if step.is_final_step %}
                <!-- 最終ステップなら、解決策テキストを表示 -->
                    {% if step.solution_text %}
                        <div class="alert alert-info solution-text">
                            <strong>解決策/メッセージ：</strong><br>
                            {{ step.solution_text|linebreaksbr }}
                    <!-- linebreaksbrで改行を<br>に変換 -->
                        </div>
                    {% else %}
                        <p class="text-musted"><em>(このステップで診断は終了です)</em></p>
                    {% endif %}
                {% else %}
                    <!-- 通常の質問ステップなら、「はい」「いいえ」ボタンを表示 -->
                    <!-- d-flex gap-3を追加 -->
                    <div class="mt-3 chart-action d-flex gap-4">
                        <button type="button" class="btn btn-light btn-yes flex-fill border"
                            data-next-step-id="{{step.yes_next_step.id | default_if_none:'' }}">はい、改善しました</button>
                        <button type="button" class="btn btn-light btn-no flex-fill border"
                            data-next-step-id="{{step.no_next_step.id | default_if_none:'' }}">いいえ、改善なし</button>
                        <!-- data-next-step-id に、次のステップのIDを埋め込んでる (JSで使う) -->
                        <!-- default_if_none:'' は、もし次のステップがNoneの場合に空文字にするフィルター -->
                    </div>
                    {% endif %}
                </div>
            </div>
            {% endfor %}

            <div class="mt-5 text-center"> {# ちょっと上に余白つけて、中央寄せにしてみる例 #}
                <button type="button" id="save-log-button" class="btn btn-lg btn-primary">
                    <i class="fas fa-save"></i> 診断を完了して記録する
                </button>
            </div>
        </div>

        <!--  ここに「診断を完了する」ボタンとかを後で追加するかも -->
        <div class="modal fade" id="confirmSaveModal" tabindex="-1" aria-labelledby="confirmSaveModalLabel"
            aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered"> <!-- modal-dialog-centered で上下中央寄せ！ -->
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="confirmSaveModalLabel">確認</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <p>この診断結果を記録しますか？</p>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">いいえ</button>
                        <button type="button" class="btn btn-primary" id="confirmSaveButton">はい、記録する</button> <!-- ←
                        このボタンにIDを付けとく！ -->
                    </div>
                </div>
            </div>
        </div>
        {% else %}
        <p class="text-center">このチャートにはまだステップが登録されていません。</p>
        {% endif %}
    </div>

    <!-- ↓↓↓ メッセージ表示モーダルのHTML ↓↓↓ -->
    <div class="modal fade" id="messageModal" tabindex="-1" aria-labelledby="messageModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered"> <!-- ← modal-dialog-centered で上下中央寄せ！ -->
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="messageModalLabel">情報</h5> <!-- ← タイトルは固定でも、JSで変えてもOK -->
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p id="messageModalText"></p> <!-- ← メッセージ本文はJSでここに入れる！ -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal"
                        id="messageModalOkButton">OK</button> <!-- ← OKボタン -->
                </div>
            </div>
        </div>
    </div>
    <!-- ↑↑↑ メッセージ表示モーダルのHTMLここまで ↑↑↑ -->


    <!-- ここにJavaScriptのコードを後で書いていく！ -->
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const startingStepId = "{{ starting_step_id_for_js | default_if_none:'' }}";
            const allStepElements = document.querySelectorAll('.chart-step');

            // ★★★ ここに回答記録用オブジェクトを追加！ ★★★
            let userAnswers = {}; // 例: { 'q1': 'yes', 'q2': 'no', 'q3': null }

            // ★★★ 初期化処理を追加 ★★★
            allStepElements.forEach(function (stepElement) {
                const stepId = stepElement.dataset.stepId;
                if (stepId) { // stepId がちゃんと取れる場合のみ
                    userAnswers[stepId] = null;
                }
            });
            console.log('userAnswers 初期状態:', JSON.stringify(userAnswers));

            // --- ① setActiveStep 関数 (これはOK！) ---
            function setActiveStep(stepIdToActivate) {
                allStepElements.forEach(function (stepElement) {
                    stepElement.classList.remove('active-step');
                    stepElement.style.display = 'block';
                    if (stepIdToActivate && stepElement.dataset.stepId === stepIdToActivate) {
                        stepElement.classList.add('active-step');
                    }
                });
            }

            // ★★★ ここに handleCompletion と handleAnswer を移動する！ ★★★
            // --- ② handleCompletion 関数 (ループの外に定義！) ---
            function handleCompletion(answeredWith) {
                const chartArea = document.getElementById('chart-area');
                if (chartArea) {
                    const existingMsg = chartArea.querySelector('.alert');
                    if (existingMsg) existingMsg.remove();
                    const completionMessage = document.createElement('div');
                    completionMessage.className = `alert alert-${answeredWith === 'yes' ? 'success' : 'info'} mt-3`;
                    completionMessage.textContent = answeredWith === 'yes' ?
                        '診断が完了しました！ (「はい」で終了)' :
                        'これ以上試せることはありません。診断を終了します。 (「いいえ」で終了)';
                    chartArea.appendChild(completionMessage);
                }
            }

            let improvementWasFound = false; // 初期値はfalse
            // --- ③ handleAnswer 関数 (ループの外に定義！) ---
            function handleAnswer(buttonType, stepElement, yesButton, noButton) {
                const currentStepId = stepElement.dataset.stepId;
                const previousAnswer = userAnswers[currentStepId]; // 以前の回答をuserAnswersから取得

                console.log(`--- handleAnswer CALLED (Refactored) ---`); // ログメッセージ少し変更
                console.log(`currentStepId: ${currentStepId}`);
                console.log(`buttonType (押されたボタン): ${buttonType}`);
                console.log(`previousAnswer (前回の回答): ${previousAnswer}`);
                console.log(`--------------------------`);


                // --- 1. 回答の更新 ---
                if (previousAnswer === buttonType) {
                    // 同じボタンを再度クリックした場合 (選択をキャンセル)
                    console.log('>>> 選択をキャンセルします <<<');
                    userAnswers[currentStepId] = null; // 回答をnull（未選択）にする
                    if (buttonType === 'yes') {
                        // 他に 'yes' の回答が残ってなければ、improvementWasFound を false に戻す
                        improvementWasFound = Object.values(userAnswers).includes('yes');
                        console.log("「はい」がキャンセルされました。improvementWasFound:", improvementWasFound);
                        updateFinalStepStyles(); // 最終ステップの見た目も更新
                    }
                } else {
                    // 新しいボタンを選択した場合 (または選択を変更した場合)
                    console.log('>>> 新しく選択、または選択を変更します <<<');
                    userAnswers[currentStepId] = buttonType; // 新しい回答を記録
                    if (buttonType === 'yes') {
                        improvementWasFound = true; // ★★★ 「はい」が選択されたらフラグをTrueに！ ★★★
                        console.log("「はい」が選択されました。improvementWasFound を True にします。");
                        updateFinalStepStyles(); // ★★★ 最終ステップの見た目を更新！ ★★★
                    } else if (buttonType === 'no' ) {// ← "いいえ" が押された場合
                        if (previousAnswer === 'yes') { // 前が "はい" だった場合 (変更)
                        // 「いいえ」が選択され、かつ前が「はい」だった場合 (「はい」から「いいえ」への変更)
                        // 他に 'yes' の回答が残ってなければ、improvementWasFound を false に戻す
                            improvementWasFound = Object.values(userAnswers).includes('yes');
                            console.log("「はい」から「いいえ」に変更されました。improvementWasFound:", improvementWasFound);
                        }
                        updateFinalStepStyles(); // 最終ステップの見た目も更新
                    }
                }

                console.log('userAnswers 更新後:', JSON.stringify(userAnswers)); // 更新後のuserAnswersをログに出力

                // --- 2. 見た目の更新 (新しい関数を呼び出す) ---
                updateStepAppearance(stepElement, yesButton, noButton, userAnswers[currentStepId]);

                // --- 3. 次のステップへの遷移ロジック ---
                if (userAnswers[currentStepId] !== null) { // 何かしら選択されている場合 (キャンセルされていない場合)
                    // 選択されたボタンに基づいて次のステップIDを取得
                    const nextStepId = (userAnswers[currentStepId] === 'yes' ? yesButton.dataset.nextStepId : noButton.dataset.nextStepId);

                    if (nextStepId) { // 次のステップIDが存在する場合
                        setActiveStep(nextStepId); // 次のステップをアクティブにする
                    } else { // 次のステップIDがない (これが最終ステップだった場合)
                        handleCompletion(userAnswers[currentStepId]); // 完了処理を呼び出す
                        setActiveStep(null); // アクティブなステップをなくす (全てのステップのactive-stepクラスを外す)
                    }
                } else {
                    // 回答がキャンセルされた場合 (userAnswers[currentStepId] が null)
                    // 特に何もしなくても、updateStepAppearanceで見た目は元に戻っているはず。
                    // もし、キャンセル時に特定のステップを再度アクティブにしたい場合は、ここでsetActiveStepを呼ぶ。
                    // (例: setActiveStep(currentStepId); )
                    // 今回はマサ君の意向で「気にしない」なので、何もしない。
                    console.log(`ステップID: ${currentStepId} の選択がキャンセルされました。`);
                }
            }

            // --- ★★★ 新しく追加する関数！ ★★★ ---
            // 指定されたステップの見た目 (ボタンの選択状態、ステップの回答済み状態) を更新する関数
            function updateStepAppearance(stepElement, yesButton, noButton, currentAnswer) {
                const stepId = stepElement.dataset.stepId;

                // まず、両方のボタンの 'selected-button' クラスを削除 (リセット)
                yesButton.classList.remove('selected-button');
                noButton.classList.remove('selected-button');

                if (currentAnswer === 'yes') {
                    // 「はい」が選択されている場合
                    yesButton.classList.add('selected-button'); // 「はい」ボタンに選択クラスを追加
                    stepElement.classList.add('answered-step');  // ステップ要素に回答済みクラスを追加
                    console.log(`ステップID: ${stepId} - 「はい」が選択された見た目に変更しました。`);
                } else if (currentAnswer === 'no') {
                    // 「いいえ」が選択されている場合
                    noButton.classList.add('selected-button'); // 「いいえ」ボタンに選択クラスを追加
                    stepElement.classList.add('answered-step'); // ステップ要素に回答済みクラスを追加
                    console.log(`ステップID: ${stepId} - 「いいえ」が選択された見た目に変更しました。`);
                } else {
                    // どちらも選択されていない場合 (null の場合)
                    stepElement.classList.remove('answered-step'); // ステップ要素から回答済みクラスを削除
                    console.log(`ステップID: ${stepId} - 未選択の見た目に変更しました。`);
                }
            }

            function updateFinalStepStyles() {
            console.log("--- updateFinalStepStyles CALLED ---");
            console.log("improvementWasFound:", improvementWasFound);
            // 全ての最終ステップのカード要素を取得 (data-solution-type 属性で判別する例)
            const allFinalStepCards = document.querySelectorAll('.chart-step[data-is-final-step="true"]'); // もっと絞り込めるセレクタが良いかも
            console.log("Found final step cards:", allFinalStepCards.length); // 見つかったカードの数もログに出す

            allFinalStepCards.forEach(function(cardElement, index) {
                 // is_final_step フラグがDjangoテンプレートからJSに渡せてない場合、
                // data属性などで最終ステップかどうかを判別する必要がある。
                // もし、allStepElements のループの中で、step.is_final_step を data属性に埋め込んでるなら、
                // ここで cardElement.dataset.isFinal === 'true' みたいな判定ができる。
                // 今回は、デモとして、IDが "final-step-card-XXX" のものだけを対象にしてみる (仮)
                // そして、それが「解決」系か「未解決」系かは data-solution-type で判断する

                const stepId = cardElement.dataset.stepId;
                const solutionType = cardElement.dataset.solutionType; // HTML側で data-solution-type を設定しておく
                console.log(`  [Card ${index}] stepId: ${stepId}, solutionType: '${solutionType}'`); // solutionTypeをクォートで囲む

                // まず、両方の枠線スタイルをリセット
                cardElement.classList.remove('final-step-improved');
                cardElement.classList.remove('final-step-unresolved');
                console.log(`    [Card ${index}] Removed classes. Current classList:`, cardElement.className);

                if (improvementWasFound) { // ★改善フラグがTrueの場合★
                    console.log(`    [Card ${index}] improvementWasFound is TRUE.`);
                    if (solutionType === 'improved') { // この最終ステップが「解決」系なら
                        cardElement.classList.add('final-step-improved');
                        console.log(`ステップID: ${cardElement.dataset.stepId} に "improved" スタイル適用`);
                        console.log(`      Applied: final-step-improved. New classList:`, cardElement.className);
                    } else {
                            console.log(`      Not 'improved' type. No class added.`);
                    }
                    // 「解決しない」系の最終ステップには、デフォルトの枠も消しておく (もし付いてたら)
                    // (else if (solutionType === 'unresolved') { cardElement.classList.remove('final-step-unresolved'); } は、
                    //  デフォルトでCSS側で final-step-unresolved が付いてるなら、ここで明示的に消す必要はないかも。
                    //  むしろ、改善されたら「解決しない」枠は絶対消したいので、上の remove でOK。)
                } else { // ★改善フラグがFalseの場合★
                    // (「解決した」枠は既に上で消されてる)
                    console.log(`    [Card ${index}] improvementWasFound is FALSE.`);
                    if (solutionType === 'unresolved') { // この最終ステップが「解決しない」系なら
                        cardElement.classList.add('final-step-unresolved'); // デフォルトの「解決しない」枠を付ける
                        console.log(`ステップID: ${cardElement.dataset.stepId} に "unresolved" スタイル適用`);
                        console.log(`      Applied: final-step-unresolved. New classList:`, cardElement.className);
                        
                    }
                }
            });
        }
        // --- ★★★ ここまでが新しく追加・修正するJavaScript ★★★ ---
            // ★★★ ページ読み込み時に、一度最終ステップのスタイルを初期設定する！ ★★★
            updateFinalStepStyles();
            console.log("ページ読み込み完了。最終ステップのスタイルを初期設定しました。");
        


            // --- ★★★ ここまでが新しく追加する関数 ★★★ ---
            // --- ④ 最初のステップ表示ロジック (ここはOK！) ---
            allStepElements.forEach(function (stepElement) {
                stepElement.style.display = 'block';
                stepElement.classList.remove('active-step');
            });
            if (!allStepElements || allStepElements.length === 0) {
                console.warn("表示するチャートステップがありません。");
            }
            // setActiveStep(null); // ← これ、もし最初のステップをアクティブにするなら、前回の if (startingStepId) ... の方がいいかも？

            // --- ⑤ イベントリスナー設定 (ループの中身は、呼び出しだけでOK！) ---
            if (allStepElements) {
                allStepElements.forEach(function (stepElement) { // このループの中はスッキリするはず！
                    const yesButton = stepElement.querySelector('.btn-yes');
                    const noButton = stepElement.querySelector('.btn-no');

                    if (yesButton) {
                        yesButton.addEventListener('click', function () {
                            handleAnswer('yes', stepElement, yesButton, noButton);
                        });
                    }
                    if (noButton) {
                        noButton.addEventListener('click', function () {
                            handleAnswer('no', stepElement, yesButton, noButton);
                        });
                    }
                });
            }
            // ★★★ 新しいボタンのイベントリスナーを追加！ ★★★
            const confirmSaveModalElement = document.getElementById('confirmSaveModal');
            let confirmSaveModal = null;
            if (confirmSaveModalElement) {
                confirmSaveModal = new bootstrap.Modal(confirmSaveModalElement);
            }
            // const confirmSaveModal = new bootstrap.Modal(confirmSaveModalElement); // BootstrapのModalインスタンスを作る！
            const confirmSaveButton = document.getElementById('confirmSaveButton'); // モーダル内の「はい」ボタンも取得

            const saveLogButton = document.getElementById('save-log-button');// これは「診断を完了して記録する」ボタン
            if (saveLogButton) {
                saveLogButton.addEventListener('click', function () {
                    console.log('「診断を完了して記録する」ボタンが押されました！');

                    // ここで userAnswers の中身を最終確認してもいいかも
                    console.log('現在の回答状況:', JSON.stringify(userAnswers));

                    // 確認ポップアップを表示
                    // const isConfirmed = confirm('この診断結果を記録しますか？'); // confirm() は true か false を返す
                    confirmSaveModal.show(); // これでモーダルが表示される！
                });
            }
            // ★★★ メッセージモーダル用のオブジェクトも取得しておく ★★★
            const messageModalElement = document.getElementById('messageModal');
            let messageModal = null;
            let messageModalLabel = null; // タイトル要素も取得
            let messageModalText = null;  // 本文要素も取得
            let messageModalOkButton = null; // OKボタンも取得 (もし何か特別な処理をさせたい場合)

            if (messageModalElement) {
                messageModal = new bootstrap.Modal(messageModalElement);
                messageModalLabel = document.getElementById('messageModalLabel'); // モーダル内の要素を取得
                messageModalText = document.getElementById('messageModalText');
                messageModalOkButton = document.getElementById('messageModalOkButton');
            }

            if (confirmSaveButton && confirmSaveModal && messageModal) {
                confirmSaveButton.addEventListener('click', function () {
                    console.log('モーダルの「はい、記録する」ボタンが押されました。サーバーにデータを送信します。');
                    console.log('送信するデータ:', JSON.stringify(userAnswers));


                    // モーダルを隠す (送信処理が始まる前に隠してもいいし、終わってからでもいい)
                    confirmSaveModal.hide();

                    const csrftoken = getCookie('csrftoken');
                    fetch("{% url 'albatross_app:save_chart_log' %}", {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                            'X-CSRFToken': csrftoken
                        },
                        body: JSON.stringify({
                            chart_type_id: "{{ chart_type.id }}",
                            answers: userAnswers
                        })
                    })
                        .then(response => {
                            if (response.ok) {
                                return response.json();
                            }
                            throw new Error('サーバーとの通信に失敗しました。');
                        })
                        .then(data => {
                            console.log('サーバーからの応答:', data);
                            if (data.status === 'success') {
                                // ↓↓↓ alert() の代わりにメッセージモーダルを表示！ ↓↓↓
                                if (messageModalLabel) messageModalLabel.textContent = '成功'; // タイトルを設定
                                if (messageModalText) messageModalText.textContent = data.message || '診断結果を記録しました！';
                                messageModal.show(); // メッセージモーダルを表示

                                // ★★★ メッセージモーダルのOKボタンが押されたらリダイレクトする処理を追加 ★★★
                                // (もし messageModalOkButton に特別な処理をさせないなら、このリスナーは不要で、
                                //  data-bs-dismiss="modal" だけでOK。リダイレクトはモーダルが隠れた後でもいい)
                                if (messageModalOkButton) {
                                    // 既存のイベントリスナーを一旦削除 (複数回登録されるのを防ぐため)
                                    const newOkButton = messageModalOkButton.cloneNode(true);
                                    messageModalOkButton.parentNode.replaceChild(newOkButton, messageModalOkButton);
                                    messageModalOkButton = newOkButton; // 新しいボタン要素を再割り当て

                                    messageModalOkButton.addEventListener('click', function handleSuccessOk() {
                                        // messageModal.hide(); // data-bs-dismiss="modal" があれば不要
                                        window.location.href = "{% url 'albatross_app:chart_type_list' %}";
                                        // イベントリスナーを削除 (一度きりの処理の場合)
                                        // messageModalOkButton.removeEventListener('click', handleSuccessOk); // これは hide.bs.modal イベントの方が良いかも
                                    }, { once: true }); // { once: true } で一度だけ実行されるリスナーにするのも手
                                } else {
                                    // もしOKボタンにリスナーを付けないなら、モーダルが閉じた後にリダイレクト
                                    messageModalElement.addEventListener('hidden.bs.modal', function onModalHidden() {
                                        window.location.href = "{% url 'albatross_app:chart_type_list' %}";
                                        messageModalElement.removeEventListener('hidden.bs.modal', onModalHidden); // リスナー削除
                                    }, { once: true });
                                }

                            } else { // 記録に失敗した場合
                                if (messageModalLabel) messageModalLabel.textContent = 'エラー'; // タイトルを設定
                                if (messageModalText) messageModalText.textContent = '記録に失敗しました：' + (data.message || '不明なエラー');
                                messageModal.show();
                                // 失敗時は特にリダイレクトとかはしない (モーダル閉じるだけ)
                            }
                        })
                        .catch(error => {
                            console.error('エラーが発生しました:', error);
                            if (messageModalLabel) messageModalLabel.textContent = '通信エラー'; // タイトルを設定
                            if (messageModalText) messageModalText.textContent = '記録中にエラーが発生しました。しばらくしてからもう一度お試しください。';
                            messageModal.show();
                        });
                });
            }
            // --- CSRFトークン取得のためのヘルパー関数 (これも DOMContentLoaded の中) ---
            function getCookie(name) {
                let cookieValue = null;
                if (document.cookie && document.cookie !== '') {
                    const cookies = document.cookie.split(';');
                    for (let i = 0; i < cookies.length; i++) {
                        const cookie = cookies[i].trim();
                        if (cookie.substring(0, name.length + 1) === (name + '=')) {
                            cookieValue = decodeURIComponent(cookie.substring(name.length + 1));
                            break;
                        }
                    }
                }
                return cookieValue;
            }
            // --- CSRFトークン取得ヘルパー関数ここまで ---
        }

        );
    </script>
    {% endblock %}
--------------------------------------------------
File: \albatross_app\templates\albatross_app\chart_type_list.html
{% extends "albatross_app/base.html" %}
{% load static %}

{% block title %}故障タイプ別選択{% endblock %}

{% block content %}
<div class="container mt-3">

    <p class="text-end">こんにちは、{{ user.username }} さん！</p>

    <h2 class="mb-4 text-center">どのようなトラブルですか？</h2>

    {% if chart_types %}

<!-- ↓↓↓ ★★★ ここに、新しいグリッドシステムのHTML構造を入れる！ ★★★ ↓↓ -->
    <div class="row g-3">
            <div class="col-12 col-sm-6 col-md-4">
                <div class="card h-100">
                    <div class="card-header">
                        電源 <!-- カテゴリ名 -->
                    </div>
                    <div class="card-body d-flex flex-column">
                        <div class="d-grid gap-2 mb-2">
                            <!--↓↓↓ href には、対応する ChartType のIDを使ったURLを入れる！  -->
                            <!--  もし「電源断」が ChartType のID 1 に対応するなら… -->
                            <a href="{% url 'albatross_app:start_chart' 1 %}" class="btn btn-outline-primary">電源断</a>
                        </div>
                        <div class="d-grid gap-2 mb-2">
                            <a href="{% url 'albatross_app:start_chart' 2 %}" class="btn btn-outline-primary">充電不可</a> <!--IDは仮 -->
                        </div>
                        <div class="d-grid gap-2">
                            <a href="{% url 'albatross_app:start_chart' 3 %}" class="btn btn-outline-primary">再起動ループ</a> <!-- IDは仮 -->
                        </div>
                    </div>
                </div>
            </div>


        <!-- 「機能」カテゴリのブロック (同様に手打ち)  -->
        <div class="col-12 col-sm-6 col-md-4">
            <div class="card h-100">
                <div class="card-header">
                    機能
                </div>
                <div class="card-body d-flex flex-column">
                    <div class="d-grid gap-2 mb-2">
                        <a href="{% url 'albatross_app:start_chart' 4 %}" class="btn btn-outline-primary">カメラ使用不可</a> <!--IDは仮 -->
                    </div>
                    <div class="d-grid gap-2">
                        <a href="{% url 'albatross_app:start_chart' 5 %}" class="btn btn-outline-primary">音不調</a> <!-- IDは仮 -->
                    </div>
                </div>
            </div>
        </div>

        <!-- 「その他」カテゴリのブロック (同様に手打ち)  -->
        <div class="col-12 col-sm-6 col-md-4">
            <div class="card h-100">
                <div class="card-header">
                    その他
                </div>
                <div class="card-body d-flex flex-column">
                    <div class="d-grid gap-2">
                        <a href="{% url 'albatross_app:start_chart' 6 %}" class="btn btn-outline-primary">挙動不審</a> <!-- IDは仮 -->
                    </div>
                </div>
            </div>
        </div>

        <!--「光」カテゴリのブロック (同様に手打ち) -->
        <div class="col-12 col-sm-6 col-md-4">
            <div class="card h-100">
                <div class="card-header">
                    光
                </div>
                <div class="card-body d-flex flex-column">
                    <div class="d-grid gap-2">
                        <a href="{% url 'albatross_app:start_chart' 7 %}" class="btn btn-outline-primary">ネット接続不可</a> <!-- IDは仮 -->
                    </div>
                </div>
            </div>
        </div>
<!-- もしカテゴリが増えるなら、同じように col のブロックを追加していく -->
    </div>
{% else %}
<p class="text-center">現在利用可能なチャートはありません。</p>
{% endif %}
</div>
{% endblock %}
--------------------------------------------------
File: \albatross_app\templates\albatross_app\_bug_base.html
<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}アルバトロス{% endblock %}</title> <!-- 各ページで上書きできるようblockタグで囲う -->

    <!-- ブートストラップ等共通で使うCSSを読み込む場所 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">



    <!-- プロジェクト共通のファイルがあればここで読み込む -->
    {% load static %}<!-- staticファイルを使うためのコード-->
    <!-- <link rel="stylesheet" href="{% static 'css/base.css' %}"> -->
    <!-- static/css/base.cssを読み込む場合 -->
    <link rel="stylesheet" href="{% static 'accounts/css/accounts_styles.css' %}">
    <link rel="stylesheet" href="{% static 'albatross_app/css/albatross_app_chart.css' %}">

    {% block extra_head %}{% endblock %}
    <!-- 各ページで追加のcssやHEADタグを入れたいとき用 -->


    <!-- <link rel="stylesheet" href="{% static 'albatross_app/css/top_styles.css' %}"> -->

</head>


<!-- 5/14ヘッダー中身追記 -->

<body>

    <header>
        <nav class="navbar navbar-expand-lg navber-light bg-light"> <!-- ここからナビゲーションバー -->
            <div class="container-fluid">
                <a class="navbar-brand fw-light text-primary fs-4"
                    href="{% if user.is_authenticated %}{% url 'albatross_app:chart_type_list' %}{% else %}{% url 'accounts:login' %}{% endif %}">
                    <!-- 以下画像の場所 -->
                    <img src="{% static 'albatross_app/images/flap.png' %}" alt="albatrossロゴ" width="40" height="40"
                        class="d-inline-block align-text-top me-2">
                    <!-- ↑↑↑ srcは仮のパス、altは代替テキスト、width/heightで仮のサイズ、classで見た目調整 -->
                    Albatross
                </a>
                <div class="ms-auto">
                    {% if user.is_authenticated %}
                    <!-- <span class="navbar-text me-3">
                        こんにちは、{{ user.username }} さん！
                    </span> -->
                    <ul class="navbar-nav flex-row">
                        <li class="nav-item me-2">

                            <button type="button" class="btn btn-outline-secondary btn-sm" data-bs-toggle="modal"
                                data-bs-target="#screenSettingsModal">
                                <i class="bi bi-gear-fill"></i> <!-- ← Bootstrap Icons の歯車アイコン -->
                                画面設定<!-- ← 大きい画面の時だけ文字も表示 (任意) -->
                            </button>
                        </li>
                        <li class="nav-item me-2">
                            <div class="btn-group"><!-- ボタン余白のためme-2追加 -->
                                <button type="button" class="btn btn-outline-secondary btn-sm dropdown-toggle"
                                    data-bs-toggle="dropdown" aria-expanded="false">アカウント設定</button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <!-- 今、まさに見ているページが「パスワード変更ページ (URL名が 'password_change')」だったら… -->
                                    <li><a class="dropdown-item" href="{% url 'accounts:password_change' %}" 
                                        {% if request.resolver_match.url_name !="password_change" 
                                        and request.resolver_match.url_name !='account_delete' %} target="_blank" {% endif %}>
                                            パスワード変更</a>
                                    </li>
                                    <!-- ↑ target="_blank" を付けない！ (同じタブで再読み込み、または何も起こらないようにする)  -->

                                    <li>
                                        <hr class="dropdown-divider">
                                    </li><!-- 区切り線 -->

                                    <!-- 今、まさに見ているページが「アカウント削除ページ (URL名が 'account_delete')」だったら… -->
                                    <li><a class="dropdown-item" href="{% url 'accounts:account_delete'%}" 
                                        {% if request.resolver_match.url_name !="account_delete" 
                                        and request.resolver_match.url_name !="password_change" %} target="_blank" {% endif %}>
                                        アカウント削除</a></li>
                                </ul>
                            </div>
                        </li>
                        <li class="nav-item">
                            <form action="{% url 'accounts:logout'%}" method="post" class="d-inline">
                                {% csrf_token %}
                                <button type="submit" class="btn btn-outline-secondary btn-sm">
                                    ログアウト
                                </button>
                            </form>
                        </li>
                    </ul>

                    {% else %}
                            <!-- 未ログインユーザー向けの表示 -->
                            <ul class="navbar-nav flex-row">
                                <li class="nav-item">
                                    <a href="{% url 'accounts:login' %}" class="btn btn-outline-primary btn-sm">ログイン</a>
                                </li>
                                <li class="nav-item ms-2">
                                    <a href="{% url 'accounts:signup' %}" class="btn btn-primary btn-sm ms-2">新規登録</a>
                                </li>
                                <!-- この「新規登録」ボタンは、user.is_authenticated が False の時だけ表示される -->
                            </ul>
                    {% endif %}
                </div>
            </div>
        </nav>
    </header>

    <main class="container mt-4"><!-- メインコンテンツ表示 -->
        {% block content %}
        <!-- ここに各ページの中身が入る -->
        {% endblock %}
    </main>

    <footer class="mt-auto py-3 bg-light text-center">
        <div class="container">
            <span class="text-muted">© 2025 アルバトロス</span>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- ↑ Popper.js も一緒に入ってるバンドル版を使うと楽ちん！ -->

    {% block extra_js %}{% endblock %}

</body>

</html>
--------------------------------------------------
File: \albatross_app\admin.py
from django.contrib import admin
from .models import ChartStep, ChartType, TroubleshootingSession, SessionLog# さっき作った ChartStep モデルをインポート！

# ChartStep モデルを管理サイトに登録
admin.site.register(ChartStep)

# ChartType モデルも管理サイトに登録
admin.site.register(ChartType)

admin.site.register(TroubleshootingSession) #5/27---追記
admin.site.register(SessionLog) #5/27---追記
--------------------------------------------------
File: \albatross_app\apps.py
from django.apps import AppConfig


class AlbatorossAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'albatross_app'

# 5/14---スペルミス修正
--------------------------------------------------
File: \albatross_app\models.py
from django.db import models
from django.contrib.auth.models import User # Userモデルをインポート
from django.utils import timezone # timezoneをインポート

# 5/27---timezoneと Userモデルをインポート


class ChartType(models.Model):
    name = models.CharField(
        max_length=100,
        unique=True,
        verbose_name="チャート種別名",
    )
    description = models.TextField(
        blank=True,
        verbose_name="チャート種別の説明",
    )
    # (任意) このチャート種別の開始ステップ (ChartStepモデルへのOneToOneField)
    # これがあると、「この種類のチャートの最初の質問はコレ！」って直接指定できて便利かも
    # starting_chart_step = models.OneToOneField(
    #     'ChartStep',
    #     on_delete=models.SET_NULL,
    #     null=True,
    #     blank=True,
    #     related_name='starts_chart_type',
    #     verbose_name="このチャートの開始ステップ"
    # )

    def __str__(self):
        return self.name

    class Meta:
        verbose_name = "チャート種別"
        verbose_name_plural = "チャート種別"

class ChartStep(models.Model):
    chart_type = models.ForeignKey(
        ChartType, # 上で定義した ChartType モデルを参照
        on_delete=models.CASCADE, # もしチャート種別が削除されたら、関連するステップも全部削除する
        verbose_name="チャート種別"
        # null=True, blank=True を付けるかどうかは、既存データとの兼ね合いで考える
        # (まだ ChartStep にデータがなければ、付けなくてもOKなはず)
    )
# 5/26---この chart_type フィールドをここに追加！ 

# 質問文
    question_text = models.TextField(verbose_name="質問文")

    # 「はい」を選んだ場合の次のステップ (自分自身のChartStepモデルを参照)
    # null=True, blank=True は、これが最後のステップで「はい」の次がない場合を許容するため
    yes_next_step = models.ForeignKey(
        "self", # 自分自身のモデルを参照
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="goes_to_yes",
        verbose_name="「はい」次のステップ",
    )

    # 「いいえ」を選んだ場合の次のステップ (同様に自分自身を参照)
    no_next_step = models.ForeignKey(
        "self",
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="goes_to_no",
        verbose_name="「いいえ」の場合の次のステップ",
    )

    # これがチャートの開始ステップかどうかを示すフラグ
    is_starting_step = models.BooleanField(
        default=False,
        verbose_name="開始ステップか",
    )

    # これが最終ステップ（解決策提示など）かどうかを示すフラグ
    is_final_step = models.BooleanField(
        default=False,
        verbose_name="最終ステップか",
    )

    # (任意) 最終ステップの場合の解決策やメッセージ
    solution_text = models.TextField(
        blank=True,
        null=True,
        verbose_name="解決策/メッセージ",
    )

    def __str__(self):
        # 質問文が長すぎる場合に備えて、最初の50文字だけ表示し、IDも付けると区別しやすい
        # もし question_text が None の可能性も考慮するなら、もう少し丁寧な処理が必要かも
        display_text = self.question_text[:50] if self.question_text else "(質問文なし)"
        type_name = self.chart_type.name if self.chart_type else "(種別未設定)"
        return f"ID: {self.id} - {display_text}..."
    
    class Meta:
        verbose_name = "チャートステップ"
        verbose_name_plural = "チャートステップ" # 管理画面での複数形表示名

# 以下6/4追加
    SOLUTION_TYPE_CHOICES = [
        ('improved', '解決/改善'),
        ('unresolved', '未解決/改善なし'),
        ('info', '情報/その他'), # 必要なら
    ]
    solution_type = models.CharField(
        max_length=20,
        choices=SOLUTION_TYPE_CHOICES,
        blank=True, # 空でもOKにするか、デフォルト値を設定するか
        null=True,  # DBレベルでNULLを許可するか
        default=None, # または 'info' とか
        verbose_name='解決タイプ'
    )

    @property
    def solution_type_for_js(self):
        return self.solution_type if self.solution_type else 'unknown'

class TroubleshootingSession(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE, verbose_name="ユーザー")
    chart_type = models.ForeignKey(ChartType, on_delete=models.CASCADE, verbose_name="チャート種別")
    end_time = models.DateTimeField(default=timezone.now, verbose_name="完了日時")
    # outcome = models.CharField(max_length=50, blank=True, null=True, verbose_name="結果") # とりあえずコメントアウト

    def __str__(self):
        return f"{self.user.username} - {self.chart_type.name} ({self.end_time.strftime("%Y-%m-%d %H:%M")})"
    
    class Meta:
        verbose_name = "トラブルシューティングセッション"
        verbose_name_plural = "トラブルシューティングセッション"
        ordering = ["-end_time"]


class SessionLog(models.Model):
    session = models.ForeignKey(TroubleshootingSession, related_name="logs", on_delete=models.CASCADE, verbose_name="セッション")
    chart_step = models.ForeignKey(ChartStep, on_delete=models.CASCADE, verbose_name="チャートステップ")
    answer = models.CharField(max_length=10, choices=[("yes", "はい"), ("no", "いいえ"), ("unknown", "不明")], verbose_name="回答")
    # answered_at = models.DateTimeField(default=timezone.now, verbose_name="回答日時") # とりあえずコメントアウト

    def __str__(self):
        return f"{self.session} - Q: {self.chart_step.id} - A: {self.get_answer_display()}"
        # get_answer_display() で choices の表示名を取れる

    class Meta:
        verbose_name = "セッションログ"
        verbose_name_plural = "セッションログ"
        unique_together = ("session", "chart_step") # 同じセッションで同じ質問には1つのログだけ
        
--------------------------------------------------
File: \albatross_app\tests.py
from django.test import TestCase

# Create your tests here.

--------------------------------------------------
File: \albatross_app\urls.py
from django.urls import path
from . import views# viewsモジュールをインポート

from django.http import HttpResponse


app_name = "albatross_app"

def some_testview(request):
    return HttpResponse("ここはalbatross_appの仮ページ")

urlpatterns = [
    # path("dummy_page/", views.some_test_view, name="some_previous_page"),
    path("charts/", views.ChartTypeListView.as_view(), name="chart_type_list"),
    path("chart/<int:chart_type_id>/", views.start_chart_view, name="start_chart"),
    path("save_chart_log/", views.save_chart_log_view, name="save_chart_log"),
]
# 5/26---ダミーページはコメントアウト
# ↑↑↑ URLは '/app/charts/' みたいになる (プロジェクトのurls.pyで 'app/' を付けてる場合)
#      name='chart_type_list' は、このURLパターンを逆引きする時の名前！
# ↑↑↑チャート種別選択画面のURLパターンをここに追加！
# ↑↑↑ URLは '/app/chart/1/' みたいになる (1 の部分はチャート種別のID)
#      <int:chart_type_id> で、URLの一部を整数としてキャプチャして、ビューの引数に渡してる！
#      name='start_chart' は、chart_type_list.html で使った名前と合わせる！
#5/27---パス3行目追加
--------------------------------------------------
File: \albatross_app\views.py
from django.shortcuts import render
from django.http import HttpResponse
from django.contrib.auth.decorators import login_required
# method_decorator をインポート！(クラスベースビューに@login_requiredを適用するため)
from django.utils.decorators import method_decorator
from django.views.generic import ListView
from .models import ChartType
from django.shortcuts import render, get_object_or_404  # get_object_or_404 をインポート！
# ChartStep もインポート！
from .models import ChartType, ChartStep, TroubleshootingSession, SessionLog
from django.http import JsonResponse
from django.views.decorators.http import require_POST
from django.contrib.auth.decorators import login_required
import json

from .models import ChartType, ChartStep

# 5/26---method_decoratorとListViewとChartType追加
# 5/26---仮ページはコメントアウト
# 5/27---JsonResponse とrequire_POSTと jsonインポート！
# 5/28---TroubleshootingSession, SessionLog 追加

"""
@login_required
def some_test_view(request):
    return HttpResponse("ここは仮ぺです(ログイン必須)")
"""


@method_decorator(login_required, name="dispatch")  # クラスベースビュー全体をログイン必須にする
class ChartTypeListView(ListView):
    model = ChartType
    template_name = "albatross_app/chart_type_list.html"
    context_object_name = "chart_types"

    # (任意) もし、リストの並び順とかを細かく制御したい場合は、queryset をオーバーライド
    # def get_queryset(self):
    #     return ChartType.objects.order_by('name') # 例: 名前順に並べる


@login_required  # もちろんログイン必須！
def start_chart_view(request, chart_type_id):  # URLから chart_type_id を受け取る
    # 指定されたIDの ChartType を取得 (もし存在しなかったら404エラー)
    chart_type = get_object_or_404(ChartType, pk=chart_type_id)

    # その ChartType に関連する全ての ChartStep を取得
    # (例: is_starting_step が True のものから順に、とか、もっと賢い取得方法も考えられるけど、
    #  まずは全部取ってきて、表示の制御はJSでやる方針なので、これでOK！)

    chart_steps = ChartStep.objects.filter(
        chart_type=chart_type).order_by("id")  # ID順で取得 (任意)

    starting_step = chart_steps.filter(is_starting_step=True).first()
    starting_step_id_for_js = starting_step.id if starting_step else None

    context = {
        "chart_type": chart_type,
        "chart_steps": chart_steps,
        "starting_step_id_for_js": starting_step_id_for_js,
    }
    return render(request, "albatross_app/chart_display.html", context)


# 5/26---"starting_step_id_for_js": starting_step_id_for_js,追加

@login_required  # ログイン必須にする
@require_POST  # POSTリクエストのみ受け付けるようにする
def save_chart_log_view(request):
    # if request.method == "POST":　# ← @require_POST を使ってるなら、このif文は無くてもOK！
    try:
        # リクエストのボディからJSONデータを読み込む
        data = json.loads(request.body)
        chart_type_id = data.get("chart_type_id")
        answers = data.get("answers")  # これが userAnswers オブジェクト

        # とりあえずコンソールに出力して確認！
        print("--- save_chart_log_view が呼ばれました！ ---")
        print(f"Chart Type ID: {chart_type_id}")
        print(f"Type of Chart Type ID: {type(chart_type_id)}")  # 型も見てみよう！
        print(f"Answers: {answers}")  # 辞書型になってるはず

        # TODO: ここで実際にデータベースに保存する処理を書く (次のフェーズ)
        if chart_type_id is None:
            # chart_type_id が None の場合はエラーレスポンス
            return JsonResponse({"status": "error", "message": "チャート種別がありません。 "}, status=400)
        try:
            # ★★★ chart_type_id が文字列で送られてくる可能性があるので、intに変換 ★★★
            chart_type_id_int = int(chart_type_id)
            chart_type_obj = ChartType.objects.get(id=chart_type_id_int)
        except ValueError:
            # intへの変換に失敗した場合 (例: chart_type_id が数値じゃない文字列だった)
            print(f"Chart Type ID を整数に変換できませんでした： {chart_type_id}")
            return JsonResponse({"status": "error", "message": "無効なチャート種別ID形式です。 "}, status=400)
        except ChartType.DoesNotExist:
            # 該当する ChartType が存在しなかった場合
            print(f"指定されたチャート種別IDが見つかりません: {chart_type_id_int}")
            return JsonResponse({"status": "error", "message": "指定されたチャート種別が見つかりません。 "}, status=404)

        # 2. TroubleshootingSession オブジェクトを作成して保存
        # (これは @login_required があるので request.user は必ず存在するはず)
        session = TroubleshootingSession.objects.create(
            user=request.user,
            chart_type=chart_type_obj
            #  end_time はデフォルトで設定される
        )
        # session.save() # .create() を使った場合は、この .save() は不要

        # 3. answers をループして SessionLog オブジェクトを作成して保存
        if not isinstance(answers, dict):
            # answers が辞書型じゃない場合はエラー
            print(f"Answers が辞書型ではありません: {answers}")
            return JsonResponse({"status": "error", "message": "回答データが不正な形式です。"}, status=400)

        logs_created_count = 0
        for step_id_str, answer_value in answers.items():
            try:
                # ★★★ step_id も文字列で送られてくるので、intに変換 ★★★
                step_id_int = int(step_id_str)
                chart_step_obj = ChartStep.objects.get(id=step_id_int)

                # answer_value が None の場合は "unknown" にする
                user_answer = answer_value if answer_value is not None else "unknown"

                # answer が choices に含まれる値かチェック (より丁寧にするなら)
                valid_answers = [choice[0] for choice in SessionLog._meta.get_field("answer").choices]
                if user_answer not in valid_answers:
                    print(f"不正な回答です: step_id={step_id_int}, answer={user_answer}")
                    # ここでエラーにするか、'unknown'として記録を続けるか選べる
                    # 今回は 'unknown' にしちゃう例
                    user_answer = "unknown"

                SessionLog.objects.create(
                    session=session,
                    chart_step=chart_step_obj,
                    answer=user_answer
                )
                logs_created_count += 1
            except ValueError:
                print(f"Step ID を整数に変換できませんでした: {step_id_str}")
                #  このエラーをどう扱うか？ (無視して続けるか、全体をエラーにするか)
                # 今回は無視してログだけ残す例
                continue
            except ChartStep.DoesNotExist:
                print(f"指定されたチャートステップIDが見つかりません: {step_id_int}")
                # これも無視して続けるか、エラーにするか
                continue

        print(f"{logs_created_count} 件のセッションログを作成しました。")

        # 成功したことを示すJSONレスポンスを返す
        return JsonResponse({"status": "success", "message": f"結果を記録しました。（{logs_created_count}件のログ）"})

    except json.JSONDecodeError:
        print("JSONデコードエラーが発生しました。")
        return JsonResponse({"status": "error", "message": "無効なJSONデータです。"}, status=400)
    except Exception as e:  # 予期せぬその他のエラーもキャッチできるように
        print(f"予期せぬエラーが発生しました：{e}")
        # import traceback # 詳細なトレースバックを見たい場合
        # traceback.print_exc()
        return JsonResponse({"status": "error", "message": "サーバー内部エラーが発生しました。"}, status=500)
    # else:
    #     # require_POST デコレータがあるので、通常ここには来ないはず
    #     return JsonResponse({"status": "error", "message": "POSTリクエストのみ許可されています。"}, status=405)
--------------------------------------------------
File: \albatross_app\__init__.py
--------------------------------------------------
File: \logs\migrations\__init__.py
--------------------------------------------------
File: \logs\admin.py
from django.contrib import admin

# Register your models here.

--------------------------------------------------
File: \logs\apps.py
from django.apps import AppConfig


class LogsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'logs'

--------------------------------------------------
File: \logs\models.py
from django.db import models

# Create your models here.

--------------------------------------------------
File: \logs\tests.py
from django.test import TestCase

# Create your tests here.

--------------------------------------------------
File: \logs\views.py
from django.shortcuts import render

# Create your views here.

--------------------------------------------------
File: \logs\__init__.py
--------------------------------------------------
File: \static\albatross_app\css\chart_styles.css
/* static/albatross_app/css/chart_styles.css */
.chart-step {
    border: 1px solid #ccc;
    /* opacity: 0.7; */
    transition: opacity 0.3s ease-in-out, border-color 0.3s ease-in-out;
}
/* .chart-step.active-step {
    border: 2px solid blue;
    opacity: 1;
    background-color: #e7f3ff;
}
.chart-step.answered-step {
    border: 1px solid #ddd;
    opacity: 0.5;
    background-color: #f8f9fa;
}
.chart-step.answered-step .btn {
    pointer-events: none;
    opacity: 0.6;
} */

--------------------------------------------------
File: \manage.py
#!/usr/bin/env python
"""Django's command-line utility for administrative tasks."""
import os
import sys


def main():
    """Run administrative tasks."""
    os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'albatross.settings')
    try:
        from django.core.management import execute_from_command_line
    except ImportError as exc:
        raise ImportError(
            "Couldn't import Django. Are you sure it's installed and "
            "available on your PYTHONPATH environment variable? Did you "
            "forget to activate a virtual environment?"
        ) from exc
    execute_from_command_line(sys.argv)


if __name__ == '__main__':
    main()

--------------------------------------------------
